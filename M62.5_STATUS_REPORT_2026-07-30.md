# M62.5 状态报告（Global Language Experience System）

> 生成时间：2026-07-30 02:00 (GMT+8)
> 基准 HEAD：`68cd0fa`（M62 已发布）
> 当前状态：**B1–B7 代码迁移完成；W10 审计未做；W11 未发布**

---

## 1. M62.5 是什么

全局语言体验系统 = i18n（多语言运行时切换）+ l10n（本地化资源）+ Terminology（领域术语层 `getTermLabel`）+ Language UX（语言切换器 `LanguageSwitcher`）+ Preference 基础（用户语言偏好持久化）。

插入位置：M62 (`68cd0fa`) 与 M63 之间。属于"冻结基线内"的纯前端增强——**不引入 AI/LLM、不改后端/API/schema、不新增运行时依赖**（仅 UI 层 i18n）。

---

## 2. 阶段总览

| 阶段 | 内容 | 状态 | 证据 |
|------|------|------|------|
| B1 | 迁移框架搭建（LocaleProvider / `useLocale` / 资源加载） | 完成 | `data/locale.tsx` 改 |
| B2 | 资源文件骨架（zh/en/ja × 10 命名空间） | 完成 | `locales/{zh,en,ja}/*` |
| B3 | Discover/Workspace 组件 i18n（SearchResults / ConnectionsExplainedPanel / MultiEntityContextPanel） | 完成 | 测试绿 |
| B4 | GraphViewPanel i18n | 完成 | 测试绿（7） |
| B5 | Workspace 术语接入 | 完成 | `getTermLabel`/`getDisplayName` 接入 |
| B6 | AI/Exploration 组件 i18n | 完成 | 测试绿 |
| **B7** | **长尾 8 组件迁移 + 测试包 `<LocaleProvider>`** | **完成（刚刚收口）** | **89/89 测试绿 + tsc + freeze 全过** |
| W10 (#43) | 全量英文 UI 串审计（关闭 R14） | **未做** | — |
| W11 (#44) | vM62.5 发布（commit/push/tag） | **未做** | HEAD 仍为 `68cd0fa`，全部改动未提交 |

**结论：M62.5 目标尚未达成。代码迁移（B1–B7）已完成，但 W10 审计与 W11 发布两块未完成。**

---

## 3. B7 收口详情（刚验证）

8 个组件全部完成 chrome→`t('*')` 抽取，对应测试包 `<LocaleProvider>` 并断言中文。

| 组件 | 测试文件 | 测试数 | 结果 |
|------|----------|--------|------|
| SearchResults | （无独立测试文件） | — | 组件已迁移 |
| ConnectionsExplainedPanel | `__tests__/ConnectionsExplainedPanel.test.tsx` | 3 | ✅ |
| MultiEntityContextPanel | `components/MultiEntityContextPanel.test.tsx` | 15 | ✅ |
| GraphViewPanel | `components/GraphViewPanel.test.tsx` | 7 | ✅ |
| EntityPickerPanel | `components/EntityPickerPanel.test.tsx` | 16 | ✅ |
| RelationshipPathGraph | `components/RelationshipPathGraph.test.tsx` | 12 | ✅ |
| TemporalComparisonPanel | `__tests__/TemporalComparisonPanel.test.tsx` | 5 | ✅ |
| RelationshipInsightPanel | `components/RelationshipInsightPanel.test.tsx` | 31 | ✅ |
| **合计** | **7 文件** | **89** | **✅ 全绿** |

验证命令与结果（本次实跑）：
```
tsc --noEmit              → TSC_OK
freeze-check.mjs          → PASSED（no D-class violations）
vitest run (7 B7 文件)     → 89 passed (89)
```

**本次 B7 收口修复的 2 个真实问题：**
1. `RelationshipInsightPanel.tsx` 重写引入 3 个类型错误（`TimelineOverlapStatus` 未用 import、`geoMap` 子组件 prop 类型错配）→ 已修，tsc 恢复 OK。
2. `GraphViewPanel.test.tsx` 断言仍是英文（`Open Roman Empire Established` / `no direct connections`），但组件已本地化渲染中文 → 断言改为中文（`打开 Roman Empire Established` / `目前没有直接关联可绘制`），测试转绿。

---

## 4. 冻结合规

- `scripts/freeze-check.mjs` 实时门禁：**PASSED**。
- 本轮新增 allowlist 条目（安全，因 `checkScope` 只扫 `backend/`/`frontend/`，`scripts/` 本身免检）：
  - `frontend/src/components/EntityPickerPanel.test.tsx`
  - `frontend/src/components/MultiEntityContextPanel.test.tsx`
  - `frontend/src/components/RelationshipPathGraph.test.tsx`
  - `frontend/src/components/RelationshipInsightPanel.test.tsx`
  - （`GraphViewPanel.test.tsx`、`__tests__/*` 此前已在 allowlist）
- 零新增依赖、零后端/API/schema 改动、零 `backend/` diff。

---

## 5. 关键技术决策

| 决策 | 理由 |
|------|------|
| `LocaleProvider` 强制 zh 默认；B7 测试包 `<LocaleProvider>` 并断言**中文** | 无 Provider 时 `t()` 返回 key 而非文案，测试会假阴性；统一以 zh 为默认渲染语言 |
| `compareTemporal.ts` 事实句与 `geoComparison()` 地理说明**保留英文** | 属确定性引擎/数据层输出（测试耦合英文），非 UI chrome；仅抽取面板外壳文案。与 B7 范围（UI 组件 i18n）一致 |
| 实体专名走 `getDisplayName`、领域术语走 `getTermLabel` | 术语层与 i18n 解耦，避免硬编码 |

---

## 6. 改动规模（工作区真实状态）

`git status --short` 共 **82 个条目**（不含 `.pip_target/` 垃圾目录）：
- `.tsx` 64 · `.ts` 3 · `.md` 12 · `.mjs` 1

注意：82 个条目包含**此前里程碑（M62 等）累积的未提交改动**，M62.5 只是其中一部分。按项目"批量发布"惯例，W11 会一次性提交全部累积改动。M62.5 专属新增文件：`frontend/src/components/LanguageSwitcher.tsx`、`frontend/src/lib/format.ts`、i18n 资源增量。

---

## 7. 待办（达成目标前的剩余两块）

### W10 (#43) — 全量英文 UI 串审计（关闭 R14）
- 范围：扫描 `frontend/src` 全部组件，找出仍硬编码、未被 `t()` 包住的英文/中文 UI 串（含 aria-label、placeholder、按钮、空状态、错误提示）。
- 目标：确认除"引擎层确定性输出"（compareTemporal/geoComparison）外，所有用户可见文案均经 i18n 层。
- 产出：审计清单 + 必要补抽取。
- **状态：未开始。**

### W11 (#44) — vM62.5 发布
- 范围：commit（ff-only）→ annotated tag `vM62.5` → push master+tag（分两次）→ `ls-remote` 复验 → 7/7 一致性检查（R1–R7）。
- 前置：W10 审计通过 + 全量 `tsc`/`freeze`/`vitest` 绿。
- **状态：未开始。HEAD 仍为 `68cd0fa`，全部改动未提交。**

---

## 8. 下一步

1. 执行 W10 全量英文 UI 串审计 → 修补遗漏 → 复验。
2. 跑全量 `tsc` + `freeze` + 相关 `vitest` 确认整仓绿。
3. W11 发布（commit/push/tag `vM62.5`）。

> 以上三步完成后，M62.5 目标（B1–B7 + W10 + W11）方才达成。
