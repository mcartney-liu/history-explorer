# M62.5 Phase 1 — W0-bis Freeze Supplement Implementation Review

> 模式：STRICT READ ONLY / IMPLEMENTATION PREPARATION REVIEW
> 基线：HEAD=vM62 (68cd0fa) · runtime=0.13.0 · W1 Closure=APPROVED · Stage B Decision=APPROVED FOR PREPARATION / NOT APPROVED FOR IMPLEMENTATION
> 本阶段仅审查与准备，未修改任何源码、未改 freeze-check.mjs、未 commit/push/tag、未进入 W3/W4 或 Stage B B1–B7。

---

## 1. Executive Summary

W0-bis 的目标：在 `scripts/freeze-check.mjs` 的 `SCOPE_ALLOWLIST` 中，精确补充 Stage B B1–B7 全量迁移**将要修改**的源码文件，使 i18n 字符串外置不被 freeze 拦截；同时严格遵守 Least Privilege（禁止 `frontend/src/components/**` 宽放、禁止 backend/dependency/runtime 放行）。

真实审计结论：

- **34 个候选顶层组件**经全量 allowlist 比对，确认**全部未被放行**（精确条目无、子目录前缀不覆盖顶层）→ 候选集合属实。
- **R14（英文 UI 串盲区）实证有效**：原 Planning Review 仅用 `\p{Han}` 中文扫描，漏掉了英文串文件。本次补充扫描发现 **3 个额外的未放行 data 文件**同样含用户可见串，须一并补入：
  - `understandingRules.ts`（英文关系"意义/视角"模板，B1）
  - `insightExport.ts`（中文导出报告文案，B1/B6）
  - `explorationStarters.ts`（英文 Discover 起始描述，B4）
  - `compareTemporal.ts`（英文时间比较句，B3 TemporalComparison）
- **W0-bis 真实集合 = 34 组件 + 4 data 文件 = 38 条精确条目**，全部为精确文件路径，无目录级扩大，无 backend/dependency/runtime 放行。

**最终判定：A. READY FOR W0-bis IMPLEMENTATION**（文件集合已核实、全部精确路径、无目录扩大、无 backend/dependency/runtime）。

> 注：PO 原候选为「34 组件 + understandingRules.ts」（35 条）。本次审查经真实 allowlist diff 将集合补全为 38 条（+3 个英文串 data 文件），这正是 W0-bis 审查应有的增值。建议 PO 确认 38 条集合后再进入执行。

---

## 2. Current Freeze Baseline

### 2.1 allowlist 匹配机制（`freeze-check.mjs` L468–475）
```js
function _scopeAllowed(file) {
  return SCOPE_ALLOWLIST.some((p) => {
    if (file === p) return true;                                  // 精确路径放行
    if (p.endsWith("/") && file.startsWith(p)) return true;       // 目录前缀放行
    return false;
  });
}
```

### 2.2 当前已放行的前端相关条目（节选，证明"无 components 宽放"）
- **精确组件条目**（顶层，已放行）：CitationList, ProvenancePanel, EntityPage, RelationshipView, RelationshipEvidence, ExplorationFlowGuide, EntityHeader, GraphViewPanel, GroundedAnswer, AIExplanationPanel, EventCausalChain, EventImpactPanel, EventNarrativeCard, EventNarrativeJourney, HistorianChat, JourneyCard, ResearchPanel, ResearchDimensionCard, ResearchReport, ResearchSummary, MultiEntitySelector, ResearchBookmarkButton, ResearchLibrary, ResearchRecommendationCard, ResearchDiscoveryPanel, EntityPageShell, LandingPage, SearchBox, ConnectionsExplainedPanel, AppShell, FeedbackWidget。
- **目录前缀条目**（仅覆盖子目录，不覆盖顶层）：
  - `frontend/src/components/discover/`（M35）
  - `frontend/src/components/exploration/`（M35）
  - `frontend/src/components/journey/`（M35）
  - `frontend/src/components/ui/`（M59）
  - `frontend/src/components/entity/`（M59-006）
  - `frontend/src/components/workspace/`（M59-009）
  - `frontend/src/components/ai/`（M59-011）
- **M62.5 已有条目**（W0 已批准，4 条精确）：`frontend/src/locales/`、`frontend/src/lib/format.ts`、`frontend/src/lib/preferences.ts`、`frontend/src/components/LanguageSwitcher.tsx`。

### 2.3 关键确认
- ✅ 不存在 `frontend/src/components/**` 宽放
- ✅ 不存在 backend 放行（本次 W0-bis 不涉及）
- ✅ 不存在 dependency 放行
- ✅ 不存在 runtime 放行（runtime 锁定 0.13.0，本次无改动）

---

## 3. Exact Missing Allowlist Files

### 3.1 34 个未放行顶层组件（候选集合，经全量比对确认缺失）
> 以下文件在 `SCOPE_ALLOWLIST` 中既无精确条目、又不被任何子目录前缀覆盖（子目录前缀只匹配 `discover/ exploration/ journey/ ui/ entity/ workspace/ ai/` 下的文件）。

| # | 文件 | 所属 Stage B 阶段 | 是否必须 |
|---|------|-------------------|----------|
| 1 | frontend/src/components/TimelinePanel.tsx | B3 | 必须 |
| 2 | frontend/src/components/Breadcrumb.tsx | B2 | 必须 |
| 3 | frontend/src/components/ConnectionsPanel.tsx | B1 | 必须 |
| 4 | frontend/src/components/ContinueExploringPanel.tsx | B3 | 必须 |
| 5 | frontend/src/components/CrossTopicBridge.tsx | B3 | 必须 |
| 6 | frontend/src/components/CrossTopicConnectionsPanel.tsx | B3 | 必须 |
| 7 | frontend/src/components/CrossTopicTopicList.tsx | B3 | 必须 |
| 8 | frontend/src/components/EmptyState.tsx | B2 | 必须 |
| 9 | frontend/src/components/EntityExplorationGuide.tsx | B3 | 必须 |
| 10 | frontend/src/components/EntitySearchBox.tsx | B2/B4 | 必须 |
| 11 | frontend/src/components/ErrorCard.tsx | B2 | 必须 |
| 12 | frontend/src/components/ExplorationJourney.tsx | B3 | 必须 |
| 13 | frontend/src/components/ExplorationPathTree.tsx | B3 | 必须 |
| 14 | frontend/src/components/ExplorationPathsPanel.tsx | B3 | 必须 |
| 15 | frontend/src/components/ExplorationTrail.tsx | B3 | 必须 |
| 16 | frontend/src/components/FeaturedTopics.tsx | B4 | 必须 |
| 17 | frontend/src/components/FirstExplorationGuide.tsx | B4 | 必须 |
| 18 | frontend/src/components/HistoryBar.tsx | B2 | 必须 |
| 19 | frontend/src/components/InterpretationPanel.tsx | B1 | 必须 |
| 20 | frontend/src/components/LoadingSkeleton.tsx | B2 | 必须 |
| 21 | frontend/src/components/MainEntityCard.tsx | B3 | 必须 |
| 22 | frontend/src/components/MultiEntityTimeline.tsx | B3 | 必须 |
| 23 | frontend/src/components/RecentExplorations.tsx | B5 | 必须 |
| 24 | frontend/src/components/RecommendationPanel.tsx | B3 | 必须 |
| 25 | frontend/src/components/RelatedEntityList.tsx | B3 | 必须 |
| 26 | frontend/src/components/SummaryPanel.tsx | B1 | 必须 |
| 27 | frontend/src/components/TemporalComparisonPanel.tsx | B3 | 必须 |
| 28 | frontend/src/components/ThemesPanel.tsx | B4 | 必须 |
| 29 | frontend/src/components/TopicComparisonPanel.tsx | B3 | 必须 |
| 30 | frontend/src/components/SearchResults.tsx | B4 | 必须 |
| 31 | frontend/src/components/EntityPickerPanel.tsx | B2/B4 | 必须 |
| 32 | frontend/src/components/MultiEntityContextPanel.tsx | B6 | 必须 |
| 33 | frontend/src/components/RelationshipPathGraph.tsx | B1 | 必须 |
| 34 | frontend/src/components/RelationshipInsightPanel.tsx | B1 | 必须 |

### 3.2 4 个未放行 data 文件（R14 英文串盲区补发现）
> 经中文 `\p{Han}` 扫描 + 英文串启发式扫描（含空格引号串）+ 关键文件精读确认。

| # | 文件 | 用户可见串类型 | 所属阶段 | 是否必须 |
|---|------|----------------|----------|----------|
| 35 | frontend/src/data/understandingRules.ts | 英文关系"意义/视角"模板（`${actorName} caused conditions that led to ${targetName}...` / `perspective: 'as cause'`） | B1 | 必须 |
| 36 | frontend/src/data/insightExport.ts | 中文导出报告文案（`关系洞察报告` `实体清单` `关系类型汇总` 等） | B1/B6 | 必须 |
| 37 | frontend/src/data/explorationStarters.ts | 英文 Discover 起始描述（`description: 'First Roman emperor, reigned from 27 BC.'`） | B4 | 必须 |
| 38 | frontend/src/data/compareTemporal.ts | 英文时间比较句（`${aName} and ${bName} overlapped for ${cmp.overlapYears} years.`） | B3 | 必须 |

### 3.3 已排除项（经审计认定无需加入 W0-bis）
| 文件/类别 | 排除理由 |
|-----------|----------|
| `frontend/src/components/discover/` `exploration/` `journey/` `ui/` `entity/` `workspace/` `ai/` 下所有文件 | 已被历史 gate 目录前缀放行，无需重复 |
| `frontend/src/data/entity/` `data/ai/` `data/workspace/` 下文件 | 已被 M59 目录前缀放行（B1 改 `entityLabels.ts` 等已覆盖） |
| `frontend/src/data/aiContext.ts` | 仅含开发者向 `throw new Error('aiContext: ...')` 内部校验串，非主 UI 文本；边界项，留 B6 执行时视情况定 |
| `frontend/src/data/{candidateUtils,pickerUtils,comparison,relationshipUtils,temporalAxis,temporalUtils,timelineUtils,interpretationFormatter}.ts` | 扫描仅命中代码注释/变量名，无用户可见 UI 串 |
| 所有 `*.test.tsx` / `components/__tests__/*.test.tsx` | 遵循 W1 既定规则：**测试文件不预加 allowlist**，修改时再单独登记 |

---

## 4. Least Privilege Validation

| 校验项 | 结果 |
|--------|------|
| 是否使用 `frontend/src/components/**` 宽放 | ❌ 否（38 条全为精确文件） |
| 是否使用 `frontend/src/components/**' 或 `frontend/src/components/*` | ❌ 否 |
| 是否引入 backend 放行 | ❌ 否 |
| 是否引入 dependency 放行 | ❌ 否 |
| 是否引入 runtime 放行 | ❌ 否 |
| 每条新增是否附 ADR 限定说明 | ✅ 是（见 §6） |

**R16（W0-bis 是否违反 Least Privilege）结论：未违反。** 38 条均为精确文件路径枚举，未出现任何目录级扩大。新增条目数量虽多（38），但每条都是 Stage B 真实将修改的确定文件，符合"精确枚举、禁止目录扩大"原则。

---

## 5. Proposed SCOPE_ALLOWLIST Changes

建议在 `SCOPE_ALLOWLIST` 数组末尾（M62.5 区块内，L465 之后、L466 `];` 之前）追加以下 38 条精确条目：

```js
  // M62.5 W0-bis (Stage B i18n migration supplement) — Frontend Freeze Revision Gate
  // (lightweight ADR-M62.5-W0bis; same mechanism as M62.5 W0). Purely additive
  // allowlist entries for i18n string externalization across Stage B B1–B7.
  // ZERO backend / schema / enum / dependency change; runtime stays 0.13.0.
  // Least Privilege: each entry is an EXACT file path — NO directory prefix.
  // ADR restriction on every entry: "仅允许 i18n 字符串外置；禁止业务逻辑修改、
  //   禁止 API 修改、禁止数据结构修改、禁止枚举修改。"
  "frontend/src/components/TimelinePanel.tsx",
  "frontend/src/components/Breadcrumb.tsx",
  "frontend/src/components/ConnectionsPanel.tsx",
  "frontend/src/components/ContinueExploringPanel.tsx",
  "frontend/src/components/CrossTopicBridge.tsx",
  "frontend/src/components/CrossTopicConnectionsPanel.tsx",
  "frontend/src/components/CrossTopicTopicList.tsx",
  "frontend/src/components/EmptyState.tsx",
  "frontend/src/components/EntityExplorationGuide.tsx",
  "frontend/src/components/EntitySearchBox.tsx",
  "frontend/src/components/ErrorCard.tsx",
  "frontend/src/components/ExplorationJourney.tsx",
  "frontend/src/components/ExplorationPathTree.tsx",
  "frontend/src/components/ExplorationPathsPanel.tsx",
  "frontend/src/components/ExplorationTrail.tsx",
  "frontend/src/components/FeaturedTopics.tsx",
  "frontend/src/components/FirstExplorationGuide.tsx",
  "frontend/src/components/HistoryBar.tsx",
  "frontend/src/components/InterpretationPanel.tsx",
  "frontend/src/components/LoadingSkeleton.tsx",
  "frontend/src/components/MainEntityCard.tsx",
  "frontend/src/components/MultiEntityTimeline.tsx",
  "frontend/src/components/RecentExplorations.tsx",
  "frontend/src/components/RecommendationPanel.tsx",
  "frontend/src/components/RelatedEntityList.tsx",
  "frontend/src/components/SummaryPanel.tsx",
  "frontend/src/components/TemporalComparisonPanel.tsx",
  "frontend/src/components/ThemesPanel.tsx",
  "frontend/src/components/TopicComparisonPanel.tsx",
  "frontend/src/components/SearchResults.tsx",
  "frontend/src/components/EntityPickerPanel.tsx",
  "frontend/src/components/MultiEntityContextPanel.tsx",
  "frontend/src/components/RelationshipPathGraph.tsx",
  "frontend/src/components/RelationshipInsightPanel.tsx",
  "frontend/src/data/understandingRules.ts",
  "frontend/src/data/insightExport.ts",
  "frontend/src/data/explorationStarters.ts",
  "frontend/src/data/compareTemporal.ts",
```

---

## 6. ADR Restrictions

每一条新增 allowlist 条目统一附以下 ADR 限定说明（写入 freeze 脚本注释，并在执行时作为变更约束）：

> **ADR-M62.5-W0bis 限定**：仅允许 i18n 字符串外置（将硬编码 UI 文本迁移至 `locales/` 资源，或经 `t()` / `getTermLabel()` / `getDisplayName()` 访问）；
> **禁止**业务逻辑修改、**禁止** API 修改、**禁止**数据结构修改、**禁止**枚举修改。
> 任何超出此范围的改动必须重新走 Freeze Revision Gate。

---

## 7. Risk Assessment

| 风险 | 状态 | 说明 |
|------|------|------|
| R14 英文 UI 硬编码风险 | 🟡 已缓解（本次补全） | 原 `\p{Han}` 规划漏掉英文串文件；本次经英文串扫描补入 `understandingRules / insightExport / explorationStarters / compareTemporal` 4 文件。残余风险：仍可能存在更隐蔽的英文串文件，建议在 W0-bis 执行时再做一次全仓英文串二次确认（Stage B W10 任务已规划）。 |
| R15 W3/W4 未完成阻塞 B1/B3 | 🔴 仍为前置（本阶段不涉及） | W0-bis 仅为 freeze 放行准备；B1/B3 仍需 W3 `getTermLabel` + W4 `getDisplayName` 三模式完成后才启动。本审查不改变 R15 状态。 |
| R16 W0-bis 违反 Least Privilege | 🟢 未违反 | 38 条全为精确文件，无目录扩大、无 backend/dep/runtime 放行。 |
| 测试文件未预加 | 🟡 已知约束 | 遵循 W1 规则，测试文件修改时单独登记；若 i18n 改动触发某 34 组件的同文件测试断言更新，该 `*.test.tsx` 需在修改时补 allowlist（与现有 M30/M34 等约定一致）。 |
| `aiContext.ts` 内部错误串 | 🟡 边界 | 开发者向错误串，非主 UI；若 B6 决定外置，执行时单独追加条目。 |

---

## 8. Implementation Gate Decision

### 判定：A. READY FOR W0-bis IMPLEMENTATION

满足全部 A 条件：
- ✅ 文件集合已核实（38 条精确文件，全部比对 `SCOPE_ALLOWLIST` 确认缺失）
- ✅ 全部精确路径（无目录级扩大）
- ✅ 无 backend / dependency / runtime 放行

### 进入 W0-bis 执行的前置确认（建议 PO 拍板）
1. 确认 38 条集合（34 组件 + 4 data 文件）为 W0-bis 执行范围。
2. 确认 `aiContext.ts` 留作 B6 执行时视情况追加（不纳入本次 38 条）。
3. 确认测试文件依 W1 约定不预加，修改时单独登记。

### 本阶段结束后仍禁止
- ❌ 不修改 `freeze-check.mjs`（仅输出建议条目，待 PO 批准后由 W0-bis 执行阶段落地）
- ❌ 不进入 W3/W4
- ❌ 不进入 Stage B B1–B7

---

## 附录：验证方法（可复现）

1. 读取 `scripts/freeze-check.mjs` L161–466 全量 `SCOPE_ALLOWLIST` 数组 + L468–475 匹配函数。
2. Glob `frontend/src/components/**/*.tsx` 与 `frontend/src/data/**/*.ts` 取全量文件清单。
3. 逐项比对：顶层组件是否被精确条目或子目录前缀覆盖 → 得 34 个缺失。
4. 中文扫描 `\p{Han}` 命中 `insightExport.ts`（+`understandingRules` 经精读确认其为英文串）。
5. 英文串启发式（含空格引号串）+ 关键文件精读 → 补 `explorationStarters.ts` `compareTemporal.ts`。
6. 其余 10 个 data 文件扫描仅命中注释/变量名 → 排除。

> 结论：W0-bis 集合已核实、精确、合规，可进入执行阶段（待 PO 批准执行动作）。
