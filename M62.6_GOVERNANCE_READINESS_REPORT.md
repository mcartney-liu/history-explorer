# M62.6 Governance Readiness Report

> 评估时间：2026-07-30 08:00 GMT+8
> 基线：`7bca32a` (vM62.5, master, clean tree)
> 模式：Read Only — 只分析，不修改代码/allowlist/CI

---

## 1. Design System Governance Audit

### 1.1 Single Source of Truth 状态

| 维度 | 状态 | 说明 |
|------|------|------|
| DS 文档是否入版本 | ✅ 已提交 | `docs/design-system/History-Explorer-Design-System-V1.md`（696 行）及归档副本 `archive/` |
| 文档是否已生效契约 | ⚠️ 部分 | 文档自述是"SSOT"，但 freeze-check 不覆盖 `docs/` 目录 — 无 CI 门禁强制对齐 |
| 命名约定对齐 | ❌ 未对齐 | 文档与代码使用完全不同命名体系（见 1.2） |

### 1.2 Design Token 一致性审计（DS V1.0 FINAL ↔ 实际 CSS）

#### 颜色

| 文档名 | DS 规范 | tokens.css | 结果 |
|--------|---------|------------|------|
| `--bg-base` | `#16130E` | `#16130E` | ✅ |
| `--bg-surface` | `#1F1B14` | `#1F1B14` | ✅ |
| `--bg-elevated` | `#2A241A` | `#2A241A` | ✅ |
| `--gold-600` | `#A8862F` | ❌ 不存在 | ❌ 缺失 |
| `--gold-500` | `#CBA135` | `#CBA135` | ✅ |
| `--gold-400` | `#D8B65E` | `--gold-hi: #DDB84A` | ❌ 名/值均不同 |
| `--text-high` | `#F2EBDD` | `--hi: #f7f5f0` | ❌ 名/值均不同 |
| `--text-mid` | `#C9BFA8` | `--mid: #8a8478` | ❌ 名/值均不同 |
| `--text-low` | `#8C8270` | `--low: #5a5650` | ❌ 名/值均不同 |
| `--text-faint` | `#5E5648` | ❌ 不存在 | ❌ 缺失 |
| `--border-subtle` | `#2E281D` | `--line: rgba(255,255,255,0.06)` | ❌ 完全不同的颜色方案 |
| `--border-default` | `#3A3324` | ❌ 不存在 | ❌ |
| `--border-strong` | `#4A412E` | ❌ 不存在 | ❌ |
| `--verified` | `#4FA784` | `#4FA784` | ✅ |
| `--unverified` | `#E0883B` | `#E0883B` | ✅ |
| `--danger` | `#D9544D` | `#D9544D` | ✅ |
| `--info` | `#5B8DB8` | `#5B8DB8` | ✅ |

**遗留海军蓝主题**：tokens.css 仍包含 `--navy-deep`、`--navy`、`--navy-card`、`--navy-surface`（DS 中不存在的旧托�），且被 `components.css` 的 `.surf-card` 样式实际使用。代码实际出产"双主题"效果——DS 规范的暖棕底 + 遗留海军蓝卡片。

#### 字体等级

| 等级 | DS 规范 | typography.css | 结果 |
|------|---------|----------------|------|
| H1 | 40px | `2rem (32px)` | ❌ 32px ≠ 40px |
| H2 | 32px | `1.3rem (~20.8px)` | ❌ 20.8px ≠ 32px |
| H3 | 24px | `1.1rem (~17.6px)` | ❌ 17.6px ≠ 24px |
| Body | 16px | `14px` | ❌ 14px ≠ 16px |
| Body SM | 14px | `13px` | ⚠️ 1px 差 |
| Caption | 13px | `12px` | ⚠️ 1px 差 |

#### Spacing

| 步骤 | DS (px) | tokens.css | 结果 |
|------|---------|------------|------|
| 1 | 4 | `--space-1: 4px` | ✅ |
| 2 | 8 | `--space-2: 8px` | ✅ |
| 3 | 12 | `--space-3: 12px` | ✅ |
| 4 | 16 | `--space-4: 16px` | ✅ |
| 5 | 24 | `--space-5: 24px` | ✅ |
| 6 | 32 | `--space-6: 32px` | ✅ |
| 7 | 48 | `--space-8: 48px` | ⚠️ 命名偏移（space-7 跳 space-8） |
| 8 | 64 | `--space-12: 64px` | ⚠️ 命名偏移 |
| 9 | 96 | ❌ 不存在 | ❌ 缺失 |
| 10 | 128 | ❌ 不存在 | ❌ 缺失 |

#### Radius

| 文档名 | DS (px) | tokens.css | 结果 |
|--------|---------|------------|------|
| r-sm | 6 | `--radius-sm: 6px` | ✅ |
| r-md | 10 | `--radius-md: 10px` | ✅ |
| r-lg | 14 | `--radius-lg: 12px` | ❌ 12 ≠ 14 |
| r-xl | 20 | `--radius-xl: 14px` | ❌ 14 ≠ 20 |
| r-pill | 999 | ❌ 不存在 | ❌ 缺失 |
| — | (无) | `--radius: 8px` | ⚠️ DS 中无对应 |

#### Shadow

| 文档名 | DS 值 | tokens.css | 结果 |
|--------|-------|------------|------|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.40)` | ❌ 不存在 | ❌ |
| `--shadow-md` | `0 4px 12px rgba(0,0,0,0.45)` | ❌ 不存在 | ❌ |
| `--shadow-lg` | `0 12px 32px rgba(0,0,0,0.50)` | ❌ 不存在 | ❌ |
| `--shadow-gold` | 金辉 | `--shadow-glow`（值不同） | ⚠️ 类似但不等 |
| — | (无) | `--shadow-card` | ⚠️ DS 中无对应 |

#### Motion

| 文档名 | DS | tokens.css | 结果 |
|--------|----|------------|------|
| `--motion-micro` | 150ms `cubic-bezier(0.22,1,0.36,1)` | ❌ 不存在 | ❌ |
| `--motion-std` | 250ms | `--animation-normal: 0.3s ease` | ⚠️ 类似但不等 |
| `--motion-emph` | 350ms | ❌ 不存在 | ❌ |
| — | (无) | `--animation-fast: 0.15s ease` | ⚠️ DS 中无对应 |

### 1.3 硬编码值审计

**硬编码 hex 颜色：26 处**
- `GraphViewPanel.tsx` — 15 处：8 种实体类型色（Person=#2563eb, Event=#dc2626 等）+ SVG 描边/填充色（#e7e3da, #fdfcfa, #ffffff 等）
- `RelationshipInsightPanel.tsx` — 4 处：时间线条色（#e8a33d, #5b8fb0, #1a1a1a, #ffffff）
- `RelationshipPathGraph.tsx` — 5 处：节点/边框色（#f4f4f5, #18181b, #52525b, #a1a1aa, #3f3f46）
- `insightExport.ts` — 2 处：HTML 导出样式（#f2ede2, #f5f5f5）

**硬编码 fontSize/padding/gap/radius/shadow**：✅ 未发现（组件均使用 CSS 类和 token 变量）

### 1.4 DS 审计结论

DS V1.0 FINAL 文档质量极高，但**与代码实现之间存在系统性漂移**：
1. **命名体系不一致**：文档用 `--text-high`，代码用 `--hi`；文档用 `--border-*`，代码用 `--line`
2. **值不一致**：25 个颜色/字体/半径/阴影/动效标记中仅约半数与文档一致
3. **遗留主题残留**：`navy-*` 海军蓝系列仍被 `components.css` 实际使用
4. **DS 文档不在 freeze 保护中**：freeze-check 不扫描 `docs/` 目录，文档可独立于代码修改
5. **26 处硬编码 hex**：集中在 SVG 图形组件中

---

## 2. Freeze Governance Audit

### 2.1 当前 SCOPE_ALLOWLIST 构成统计

| 类别 | 估计条目数 | 跨越里程碑 | 典型模式 |
|------|-----------|-----------|---------|
| 后端数据层 | ~12 | M24–M29 | 单个 `.py` 文件 |
| 后端 AI Gateway | ~6 | M36 | 单个文件 |
| 后端测试修补 | ~2 | M35–M36 | 单个测试文件 |
| 前端探索/实体 UX | ~30 | M30–M35 | 单个 `.tsx` 文件 + 目录前缀 |
| 前端 AI/研究 | ~30 | M36–M41 | 单个 `.tsx` 文件 |
| 前端智能分析层 | ~30 | M42–M57 | 单个 `data/*.ts` + 测试 |
| 前端设计系统 | ~20 | M59 | 目录前缀 (`ui/`, `entity/`, `styles/`, `__tests__/`) |
| 前端搜索/落地页 | ~5 | M60 | 单个文件 |
| 前端清理 | ~2 | M61 | 单个文件 |
| 前端国际化 (M62.5) | ~45 | M62.5 | 约 40 个精确文件路径 + 目录前缀 |
| **合计** | **~130+** | **15+** | |

### 2.2 历史遗留项

以下条目在基线冻结中已无实际保护价值：

| 条目 | 原因 |
|------|------|
| `backend/app/core/dataset.py` (M24) | 数据层已稳定，后续无改动预期 |
| `backend/tests/test_dataset_*.py` × 6 | 测试稳定，非风险点 |
| `data/sources.json` / `data/evidence_claims.json` (M26) | 数据文件在 freeze 保护机制之外（freeze-check 不扫描 `data/`） |
| `backend/app/core/provenance_index.py` (M29) | 只读投影模块 |
| `ConnectionsExplainedPanel.tsx` (M61) | 仅死代码移除，已完成 |
| M62.5 精确路径 ~40 条（W0-bis） | i18n 字面量外置已完成，这些文件可收回到目录级 |

### 2.3 M62.5 新增

- `frontend/src/locales/`（目录前缀）
- `frontend/src/lib/format.ts` / `preferences.ts`（精确）
- `frontend/src/components/LanguageSwitcher.tsx`（精确）
- `frontend/src/components/__tests__/`（目录前缀）
- M62.5 W0-bis：~40 个精确组件路径（B1–B7 i18n 外置目标文件）

### 2.4 未来必然增加的类别（Experience Blueprint）

| 类别 | 预计条目数 | 理由 |
|------|-----------|------|
| Exploration OS 壳组件 | ~10–20 | 左轨/右面板/底时间线等新组件 |
| 合并后组件 | ~5–10 | DS Chapter 10 MERGE 决策落地 |
| 测试文件 | ~5–15 | 新组件的 co-located test |
| 潜在数据层 | ~5 | 如果 Experience Blueprint 需要新数据源（需 Gate） |

### 2.5 Freeze 治理结论

1. **允许列表气球化风险属实**：130+ 条目/15 里程碑，大部分为精确路径，"冻结"的约束力被逐次例外稀释
2. **目录前缀 vs 精确路径不一致**：M59 用目录前缀（ui/, entity/, styles/），M62.5 用精确路径 — 同一机制两套风格
3. **无 TTL/到期清理机制**：M24 的数据层条目 15 个里程碑后仍在列表中
4. **"一次性"条目无退役策略**：M61 的死代码移除、M62.5 的 i18n 外置完成后不应继续占位

---

## 3. Release Governance Audit

### 3.1 当前流程矩阵

| 流程 | 当前方式 | 人工/CI | 可否自动化 |
|------|---------|---------|-----------|
| 版本号更新 | 手动编辑 `frontend/package.json` | 人工 | 可—CI 自动 bump |
| CHANGELOG 条目编写 | 手动 | 人工 | ⚠️ 半可—模板化但内容需人写 |
| README 版本同步 | 手动 | 人工 | 可—CI 自动更新 |
| PROJECT_CONTEXT §5 同步 | 手动 | 人工 | 可—CI 自动更新 |
| 创建 annotated tag | `git tag -a` | 人工 | 可—CI 创建 |
| 双分次 push | `git push origin master` + `git push origin <tag>` | 人工 | ⚠️ 半可—CI 可取代但 MITM 环境不兼容 |
| Release Consistency Check | CI job（continue-on-error: true） | CI | ✅ 已是 CI |
| ls-remote 复验 | 手动 | 人工 | 可—CI 自动验证 |
| PO 拍板 | 人工决策 | 人工 | ❌ 必须人工 |

### 3.2 可 CI 自动化的流程

1. **README / PROJECT_CONTEXT / CHANGELOG 版本引用自动更新** — 在 release tag push 时自动替换为最新版本号，减少人工遗漏风险
2. **Consistency Check 升为 required status** — 当前 `continue-on-error: true` 使其在 CI 中形同虚设。改为 required 后，一致性成为阻塞门禁
3. **ls-remote 自动验证** — tag push 后 CI 可自动 `git ls-remote --tags origin` 验证 tag 已推远端

### 3.3 必须人工的流程

1. **PO 拍板**：哪些功能进发布、什么时机发布、版本号选择——永远需要有人做这个决定
2. **tag message 编写**：人类可读的发布说明，无法由 CI 代写
3. **MITM 环境下的 push**：当前公司代理环境要求特殊的 `git -c http.sslBackend=openssl -c http.sslVerify=false` 命令，CI runner 未必能复现

### 3.4 Release 治理结论

1. Release Consistency Check 是项目最完善的自动化门禁，但**仅 advisory** 使其失去阻断力
2. 7/7 校准涉及的 R4/R5/R6（README/PROJECT_CONTEXT/CHANGELOG）依赖人工可错复制粘贴
3. 前 6 个 post-M62.5 commit（非里程碑修复）已另起新基线，但未触发版本清洗——基线已从 vM62.5 漂移

---

## 4. Architecture Readiness

### 问题：Experience Blueprint 前最大的三个工程治理障碍

---

**障碍 #1 —— 冻结基线在膨胀中失去约束力**

> 严重度：P0

freeze-check 的 SCOPE_ALLOWLIST 从 M24 的 2 个文件膨胀到今日 130+ 条目。每里程碑的例外叠加，使得"冻结=默认禁止"已退化为"冻结=精确放行的文件列表"。新条目不断增加，旧条目从不清理——到 Experience Blueprint 时，allowlist 预计突破 200 条目。届时一个"默认冻结"的声明会成为纸面表述而非实际约束。

这不是孤立问题：allowlist 膨胀 → CI 门禁可读性下降 → 开发者在发 PR 前更难自我审查 → 冻结违规更难被早期发现。

**障碍 #2 —— DS 规范与代码实现存在系统性双轨**

> 严重度：P0

DS V1.0 FINAL 文档定义了暖棕底、古金、语义色的完整视觉体系，但实际 CSS 中有 25 个标记不匹配、遗留海军蓝主题仍在运行、3 个组件含 26 处硬编码 hex。这意味着：

- 一个基于 DS 文档设计的 Experience Blueprint 新组件，放到现有代码中会出现不一致
- 现有组件看的是老 token，新组件看的是 DS 文档——谁才是 SSOT？
- 合并后（DS Chapter 10）的组件该用哪套 token？

**障碍 #3 —— 无 token 级/DS 合规 CI 门禁**

> 严重度：P1

当前 CI 有 6 道门禁（typecheck/test/build/freeze/visual/structure/emoji），没有任何一项检查：

- 组件是否使用了正确的 DS token
- 颜色是否来自 token 而非硬编码
- 字体/间距/圆角是否与 DS 等级一致
- DS 文档修���是否需要同步 freeze-check

这意味着 Experience Blueprint 第一阶段可以写出"功能对但视觉漂移"的代码，事后治�用成本将数倍于事前检查。26 处硬编码 hex 就是一个缩影——它们全部是通过多轮 CI 绿了的代码。

---

## 5. 建议

### 立即（Immediate）— 零代码，仅决策

| # | 行动 | 优先级 | 涉及 |
|---|------|--------|------|
| 1 | **裁定 DS SSOT 优先级**：DS 文档优先还是 tokens.css 优先？这个决策决定后续所有 token 对齐方向 | P0 | PO + 设计 |
| 2 | **为 freeze-check allowlist 设置退役机制**：每个 milestone 条目附加 TTL（例如"此条目在 2 个里程碑后自动失效"）| P0 | 架构 |
| 3 | **禁止新增精确路径条目**：未来 allowlist 只接受目录前缀 | P1 | 架构/运维 |

### 下一里程碑（Next Milestone）— 可规��为工程任务

| # | 行动 | 优先级 | 估算 |
|---|------|--------|------|
| 4 | **将 Release Consistency Check 改为 required status**（取消 continue-on-error） | P0 | 1 行 YAML 改动 |
| 5 | **对齐 tokens.css 与 DS 规范**：逐 token 确认、补齐缺失（gold-600, text-faint, shadow-*, motion-*, radius-pill, spacing-9/10）| P0 | 中型任务 |
| 6 | **退役 components.css 中的 `--navy-*` 引用**：切换至 DS earth-tone 标记 | P1 | 中型任务，需协调 |
| 7 | **PR 模板增加 DS 合规检查 checkbox**：防新组件引入硬编码 | P1 | 轻量 |

### 长期（Long Term）— 体系性建设

| # | 行动 | 优先级 | 路径 |
|---|------|--------|------|
| 8 | **引入 token 级 CI 门禁**：扫描 `.tsx/.ts` 中的裸 hex、非 token 颜色值 | P1 | 参考 visual-check.mjs 模式扩展 |
| 9 | **DS 文档入 freeze-check 保护**：任何对 DS 文档的修改需要对应 allowlist 更新 | P2 | 需扩展 freeze-check scope |
| 10 | **allowlist 按 milestone 分文件**：每里程碑独立的 allowlist 文件，附带到期声明 | P2 | 架构重构 |
| 11 | **建立 release 自动化管线**：tag → 版本同步 → consistency check → push | P2 | 运维，当前 MITM 环境有障碍 |

---

## 附录：审计方法

| 审计项 | 方法 |
|--------|------|
| DS 文档与 token 对比 | 人工逐行对照 DS V1.0 FINAL Part 2 vs `tokens.css`/`typography.css` |
| 硬编码 hex 扫描 | Python regex `#[0-9a-fA-F]{6}` 全量扫描 `frontend/src/`（排除 `styles/` + 注释） |
| 硬编码尺寸扫描 | Python regex 扫描 `fontSize`/`padding`/`margin`/`gap`/`borderRadius`/`box-shadow` |
| Allowlist 分类 | 人工分类 SCOPE_ALLOWLIST 130+ 条目按里程碑/区域 |
| Release 流程审计 | 通读 `release-consistency-check.mjs` + `.github/workflows/ci.yml` |
| DS 入版确认 | `git ls-files docs/design-system/` |
