# Dependencies
node_modules

# Lockfile (install locally for reproducible deps)
package-lock.json

# Production build output
dist
# Phase 1 build / preview output (vite build --outDir; contains index.html + assets/)
.phase1-build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
*.local

# Playwright (M73 Phase3-A — local Alpha regression infra)
test-results/
playwright-report/
