# M62.5 Stage B Migration Decision Record

> 状态：**APPROVED FOR PREPARATION — NOT APPROVED FOR IMPLEMENTATION**
> 模式：STRICT READ ONLY / DECISION RECORD ONLY
> 基线：vM62（HEAD `68cd0fa`）｜ runtime 0.13.0 ｜ W1 已完成且未 commit
> PO Decision：APPROVED (方案 B — 补充规划后进入 Implementation)
> 本文件为决策记录，**不含任何实现代码**，不修改 `scripts/freeze-check.mjs`、不 commit / push / tag

---

## 1. PO Decision

**APPROVED — 方案 B（补充规划后进入 Implementation）**

- 不直接进入 Stage B Migration。
- 先完成 **W0-bis（Freeze 补充 Gate）** 与 **Stage A 收尾扩展（W3 术语接入 + W4 专名显示策略）**，再启动 B1→B7。
- 批准后的执行顺序见 §3。

---

## 2. Reasoning（决策理由）

### 2.1 W1 已完成（Stage A 地基已立）

经 W1 Implementation + W1 Closure Gate 验证，以下地基已具备：

| 能力 | 状态 | 证据 |
|---|---|---|
| Locale Engine（zh/en/ja 注册表 + `lookup` fallback 链） | ✅ | `data/locale.tsx` 改造完成 |
| locales Resource Framework（zh/en/ja 9 模块 × 命名空间 seed） | ✅ | `locales/{zh,en,ja}/*` + `index.ts` |
| Preference Foundation（统一 `he-prefs` + 幂等迁移） | ✅ | `lib/preferences.ts` |
| LanguageSwitcher（LUX-01 三语 UX） | ✅ | `components/LanguageSwitcher.tsx` |
| Intl Format Layer（O6 日期/数字） | ✅ | `lib/format.ts` |
| Freeze Gate（W0 allowlist 4 条） | ✅ | `freeze-check.mjs` L462-465，PASS |

W1 成功标准 = "语言系统基础设施可运行"（**非全站多语言化**），已达成。

### 2.2 Stage A 仍存在两个必要收尾（阻塞 B1/B3）

| 缺口 | 现状（READ ONLY 核实） | 阻塞影响 |
|---|---|---|
| **W3 — `getTermLabel(id, locale)` 未实现** | 全仓 grep `getTermLabel` = **0 命中**；`terminology.ts` 仅建立 9 概念数据，无访问函数 | B 批术语迁移（§2.6、AC-10）无消费入口 |
| **W4 — `getDisplayName()` 仍为 no-op** | `lib/preferences.ts:103-109` 仅 `return name`，三模式未实装；Axis 2（`contentDisplayMode`/`properNameMode`）未接线到渲染 | B1 实体专名呈现、AC-06 / AC-LUX-03 不可达 |

### 2.3 边界确认（防混淆铁律）

| 层 | 责任 | 入口 | 现状 |
|---|---|---|---|
| **`terminology.ts`** | 领域概念（Entity/Relationship/Timeline/…） | `getTermLabel(id, locale)` | 数据已建，函数待 W3 |
| **`entityLabels.ts`** | 实体类型（Person/Location/Event/…） | `getEntityLabel(type, locale)` | ⚠️ 现有签名 `getEntityLabel(type: string): string` **无 locale 参数**，需 W3/B1 扩展 |
| **`getDisplayName()`** | 实体专名展示策略 | `ProperNameMode`（original/localized/dual） | 透传 no-op，待 W4 |

---

## 3. Updated Execution Order（更新后执行顺序）

### Phase 1 — W0-bis Freeze Supplement Gate

- **目标**：补充 Stage B 可能涉及的精确 allowlist 条目。
- **原则**：
  - 🔴 **禁止** `frontend/src/components/**` 宽放（Least Privilege 铁律）。
  - 精确枚举顶层未列组件（审计约 **~34 个**顶层 `components/*.tsx`，见附录 A）。
  - 每条 allowlist 须附 ADR 限定语："仅 i18n 字符串外置，禁业务/API/枚举改动"。
- **不改动**既有 4 条 W0 allowlist（locales/、lib/format.ts、lib/preferences.ts、components/LanguageSwitcher.tsx 已放行，保持）。

### Phase 2 — Stage A Completion Extension

#### W3 — Terminology Access Layer

- 新增 `getTermLabel(id, locale)`：读 `terminology[id].labels[locale]`，缺失回退 `zh`。
- 职责：领域概念术语，**禁止**实体类型混入。
- 同步扩展 `entityLabels.ts` → `getEntityLabel(type, locale)`（补 locale 参数，回退 `zh` 或原 type 串）。

#### W4 — Proper Name Display Policy

- 正式实装 `getDisplayName(name, locale, mode)` 三模式：
  - `original` → 仅源值（如 `孔子`）
  - `localized` → 本地化层（无 `localized_name` 字段时回退 original）
  - `dual` → `孔子 / Confucius`（默认）
- 接线 Axis 2：`contentDisplayMode` / `properNameMode` → 驱动实体/内容专名渲染。
- 职责：实体专名展示策略，数据值**永不进 `t()`**。

### Phase 3 — Stage B Migration（B1 → B7）

固定顺序，每批 `freeze-check + emoji-scan + tsc + vitest` 全绿再下一批：

| 顺序 | 批次 | 内容 |
|---|---|---|
| B1 | 实体/关系 | Entity Label 本地化 + 关系解释模板 + 专名呈现 |
| B2 | 核心壳层 | App / AppShell / nav / tab 外置 + LanguageSwitcher 集成 |
| B3 | 实体页 | EntityHero / EntityHeader / RelationshipView |
| B4 | 发现页 | DiscoverPage / 子组件 |
| B5 | 工作台 | Workspace / Research* / HistorianChat |
| B6 | AI 面板 | AIExplanationPanel / PROMPT_MODES（AI 文案外置 + AI 偏好契约灰显，**不动 `aiClient.ts`**） |
| B7 | 长尾 | 顶层 `components/*.tsx` 未列者（W0-bis 白名单覆盖者） |

---

## 4. Stage B Preconditions（进入 B1 前必须满足）

| # | 前置项 | 当前状态 | 归属阶段 |
|---|---|---|---|
| [ ] | W0-bis Freeze Gate PASS | pending | Phase 1 |
| [ ] | `getTermLabel` 完成 | pending（0 命中） | Phase 2 W3 |
| [ ] | `getDisplayName` 三模式完成 | pending（no-op） | Phase 2 W4 |
| [ ] | Proper Name Policy 接线完成 | pending | Phase 2 W4 |
| [ ] | Entity Type Label 与 Terminology 边界确认 | ⚠️ 部分（terminology 已建；`getEntityLabel` 缺 locale 参数待扩） | Phase 2 W3 |
| [ ] | 中文硬编码扫描完成 | 部分（W1 审计 ~147 tsx；全量待 W0-bis 复扫） | Phase 1 |
| [ ] | 英文 UI 硬编码二次扫描完成 | pending（R18：首轮 `\p{Han}` grep 漏英文串，真实迁移面 >102 文件） | Phase 1 |

> **进入 B1 硬门禁**：以上 7 项全部勾选前，禁止启动任何 B 批代码修改。

---

## 5. Freeze Impact（冻结影响）

- **模式**：`scripts/freeze-check.mjs` 默认全冻结，仅 `SCOPE_ALLOWLIST` 放行。
- **W0 既有 4 条 allowlist（保持不动）**：
  ```
  "frontend/src/locales/",
  "frontend/src/lib/format.ts",
  "frontend/src/lib/preferences.ts",
  "frontend/src/components/LanguageSwitcher.tsx",
  ```
- **W0-bis 增量（Phase 1 执行）**：仅精确枚举顶层未列组件 + `understandingRules.ts`（关系解释模板），**禁止** `frontend/src/components/` 目录前缀宽放。
- **零触碰**：backend / API / schema / 枚举 / dependency / runtime(0.13.0) 全部不变。
- **O5 边界**：`aiClient.ts` 不接收 language 字段，AI 面板迁移仅外置文案 + 灰显契约。
- **结论**：纯前端组合，唯一必要动作 = 一次轻量 Freeze Supplement Gate（ADR + 精确 allowlist 增量），机制同 M30-A / M61。

---

## 6. Risk Update（风险更新）

### 6.1 既有风险（Rev2 R1–R13）结转状态

| ID | 风险 | 决策时状态 |
|---|---|---|
| R1 | Freeze 未放行即提交 | 🟢 受控（W0 已 PASS；W0-bis 将续补） |
| R2 | AI 语言不可达（O5） | ✅ 已移出范围（LUX-05 仅契约） |
| R3 | 测试大规模断裂 | 🟠 待 W10 统一迁移 |
| R4 | 字典键不一致 | 🟠 受控（§2.5 Key Convention） |
| R5 | 运行时切换遗漏 | 🟠 待全量 grep 硬编码串 |
| R6 | 专有名词误译 | 🟢 受控（§2.7/2.8 铁律） |
| R7 | 单文件超 300 行 | 🟢 受控（资源外置） |
| R8 | 新增依赖诱惑 | 🟢 受控（零依赖约束） |
| R9 | 资源体积 | 🟢 受控（模块化铺路） |
| R10 | 术语与 UI 文本混用 | 🟠 待 W3 `getTermLabel` 落地 |
| R11 | 偏好迁移失败 | 🟢 受控（幂等迁移 + 回归） |
| R12 | 双轴概念混淆 | 🟠 待 W4 接线 + Code Review |
| R13 | 散落 localStorage 复发 | 🟢 受控（禁令 + 门禁） |

### 6.2 新增风险（Stage B Planning Review 审计 + 本决策引入）

| ID | 风险 | 等级 | 触发条件 | 缓解 |
|---|---|---|---|---|
| **R14** | **迁移面低估（英文串盲区）** | 🟠 Med | 首轮 `\p{Han}` grep 漏英文硬编码 UI 串，真实迁移面 >102 文件 | W0-bis 二次扫描（英文 UI 串专项）；以文件枚举而非正则估算 |
| **R15** | **W3/W4 未完成阻塞 B1/B3** | 🔴 High | `getTermLabel` 缺失 / `getDisplayName` no-op 时启动 B 批 → 无消费入口 | 本决策已将其列为 Phase 2 前置，B1 硬门禁拦截 |
| **R16** | **W0-bis 枚举 Least Privilege 压力** | 🟠 Med | ~34 顶层组件精确枚举易诱导向 `components/` 宽放 | 铁律禁止宽放；每条精确条目 + ADR 限定语；CI `freeze-check` 拦截 |

---

## 7. As-Built vs Plan 偏差记录（供 W3/W4 实现参考）

| 项 | Rev2 计划 | 实际 W1 落地 | 后续动作 |
|---|---|---|---|
| `AppPreferences` 字段 | `contentMode` | `contentDisplayMode` + `properNameMode`（更细） | W4 接线以此为准 |
| `getDisplayName` 签名 | `getDisplayName(value, {mode, localized?})` | `getDisplayName(name, _locale, _mode)` no-op | W4 实装三模式 |
| `getEntityLabel` 签名 | `getEntityLabel(type, locale)` | `getEntityLabel(type: string): string`（无 locale） | W3/B1 扩展补 locale 参数 |
| `terminology` 结构 | `TERMINOLOGY` Record + `definition` 字段 | `terminology` 扁平对象（Entity/Relationship/… 直接含 zh/en/ja） | `getTermLabel` 读 `terminology[id].labels[locale]` |

---

## 8. Final State

```
PO Decision:        APPROVED (方案 B)
Preparation:        APPROVED  ← W0-bis + W3/W4 可启动规划与准备
Implementation:     NOT APPROVED  ← 禁止进入 B1–B7 代码修改
```

**等待**：W0-bis + W3/W4 全部完成后，重新提交 Stage B Entry Gate 复核，通过后方可进入 Stage B Implementation。

---

## 附录 A — Real Baseline Verification（READ ONLY 证据）

| 核查项 | 命令 / 位置 | 结果 |
|---|---|---|
| W1 文件存在 | `frontend/src/lib/preferences.ts`、`lib/format.ts`、`components/LanguageSwitcher.tsx`、`locales/terminology.ts`、`locales/index.ts` | ✅ 存在 |
| `getTermLabel` 未实现 | Grep `getTermLabel` in `frontend/src` | **0 命中** |
| `getDisplayName` no-op | `lib/preferences.ts:103-109` | `return name`（透传） |
| `getEntityLabel` 无 locale | `data/entity/entityLabels.ts:29` | `getEntityLabel(type: string): string` |
| terminology 9 概念 | `locales/terminology.ts` | Entity/Relationship/Timeline/Journey/Trust/Evidence/Historian/Discovery/Knowledge |
| Freeze allowlist | `scripts/freeze-check.mjs:462-465` | 4 条精确条目，PASS |
| W1 测试 | `vitest run` | 105 files / 941 tests PASS（W1 报告） |

> 本附录所有事实来自当前工作树实时读取，未做推断。
