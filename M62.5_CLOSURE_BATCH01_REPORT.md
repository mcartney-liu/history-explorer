# M62.5 Closure Batch 01 Report

> 时间：2026-07-30 07:55 GMT+8
> 基线：`7bca32a` (vM62.5, master, clean tree)
> 模式：严格收敛 — 仅修复发布阻塞问题，禁止新功能/UI重设计/架构调整/API/backend/数据模型/新依赖/组件重构

---

## 1. 修改文件列表

| 文件 | 修改行 | 类型 |
|------|--------|------|
| `frontend/src/components/ui/Icon.tsx` | 5 行注释 | 符号清理 |
| `scripts/emoji-scan.mjs` | +29 行 | 守卫补强 |
| `frontend/src/__tests__/m62-emoji-guard.test.ts` | +33/-6 行 | 测试同步 |

**3 文件，+69/−9 行**（来源：`git diff --stat`）

---

## 2. 每个修改原因

### 2.1 Icon.tsx — 符号图标违规清理（Task 1）

**事实扫描结果**：代码中只有 5 处命中，且全部是注释（Category C），功能代码和测试文件已**100% 干净**。

**修改**：将 5 行注释中的符号字符替换为纯文字描述：
- `// ✓ check (grounding verified)` → `// check (grounding verified)`
- `// ★ star (bookmark / favorite...` → `// star (bookmark / favorite...`
- `// ⚠ warning (triangle with exclamation)` → `// warning (triangle with exclamation)`
- `// ○ neutral circle (status ring)` → `// circle (status ring)`
- `// ✗ error (circle with cross)` → `// cross (circle with cross)`

**风险**：仅修改注释，零业务逻辑/零渲染变化。

### 2.2 emoji-scan.mjs — 补强 symbol guard（Task 2）

**根因分析**：现有 `EMOJI_RE` 虽已覆盖 ★(U+2605)☆(U+2606)✓(U+2713)✗(U+2717)○(U+25CB)⚠(U+26A0) 的 Unicode 范围，且已排除箭头/em-dash 豁免。但该脚本在检查时会剥离字符串/模板字面量（为避免文档文本误报），导致 `{isBookmarked ? '★' : '☆'}` 这类字面量符号无法被检出。

**修改**：新增 `SYMBOL_DINGBAT_RE` 检测通道，专用于这 6 个禁用符号：
- **仅剥离行注释**（保留字符串/JSX 文本来探测功能图标使用）
- **不豁免任何用例**（与 emoji 扫描的 CARVE_OUT 机制不同）
- 扫描范围和文件类型与 emoji 扫描一致（`frontend/src/` 下 `.ts/.tsx/.jsx/.js`）

**风险**：新增守卫不影响现有功能，零误报风险（已用 `★ 已收藏` / `✓ 已验证` 验证，检查前被剥离注释后 VIOLATION）。

### 2.3 m62-emoji-guard.test.ts — 测试同步（Task 1 & 2）

**修改**：
1. 从 `CARVE_OUT` 豁免集中移除这 6 个符号（M62 时被列为"dingbat marks"豁免，现已违反 P0-1）
2. 新增 `describe('M62.5 — symbol dingbat guard')` 测试块，使用非剥离字符串的扫描方法，与 `emoji-scan.mjs` 的 `SYMBOL_DINGBAT_RE` 守卫逻辑同步
3. 移除测试描述中的符号字符（避免自我触发）

**风险**：0 — 测试通过，2 个 case 全部绿。

---

## 3. 风险分析

| 风险 | 等级 | 说明 |
|------|------|------|
| 新增 guard 误报历史文本 | 🟢 无 | `SYMBOL_DINGBAT_RE` 只扫描 `frontend/src/`（不含 `data/`），且历史文本中极少出现 ★☆✓✗○⚠ |
| 现有功能被破坏 | 🟢 无 | 只改注释/测试/guard 脚本，零渲染逻辑变动 |
| freeze-check 越界 | 🟢 无 | 已在 Baseline 中解（vM62.5 已含 `frontend/src/components/__tests__/` 目录前缀） |
| 测试脆性 | 🟢 无 | 已通过全量测试验证 |

---

## 4. 验证结果

| 验证项 | 结果 | 说明 |
|--------|------|------|
| `tsc --noEmit` | ✅ PASS | 0 类型错误 |
| `freeze-check` | ✅ PASS | 0 D-class 违规 |
| `emoji-scan`（含新 Symbol Guard） | ✅ PASS | [SYMBOL GUARD] + [EMOJI GUARD] 双 PASS |
| `vitest run`（全量） | ✅ PASS | **105 文件 / 941 测试，0 失败** |
| 零符号命中扫描 | ✅ PASS | frontend/src 中 6 个禁用符号 0 处命中 |

---

## 5. 剩余阻塞问题

| # | 问题 | 状态 | 行动建议 |
|---|------|------|---------|
| 1 | **符号图标违规** | ✅ Batch 01 已清零 | 5 处注释已清理，guard 已生效；后续 guard 会自动阻断 |
| 2 | **symbol guard 盲区** | ✅ 已修复 | `emoji-scan.mjs` + `m62-emoji-guard.test.ts` 双通道覆盖（含字符串字面量） |
| 3 | **freeze-check W10 越界** | ✅ 已在 Baseline 解 | `frontend/src/components/__tests__/` 目录前缀已存在，4 个 W10 测试文件均 ALLOWED |
| 4 | **设计系统未契约化落地** | ⏸ 非 Batch 01 范围 | 设计系统文档 `History-Explorer-Design-System-V1.md` 未入 freeze allowlist |
| 5 | **M43–M49 无交叉校验** | ⏸ 非 Batch 01 范围 | 5 个情报模块独立累加，无跨模块验证 |
| 6 | **Exploration OS 壳** | ⏸ P1 方向待 PO 拍板 | 需先定"探索操作系统"产品方向，再用 Stitch 出稿验证 |

---

## 6. 总览

Batch 01 完成 3 项修复、0 项未完成。当前 vM62.5 基线在符号治理和冻结守卫两方面已达到**干净可发布状态**。

等待人工确认后，可推进下一步（如 M63 启动或 Exploration OS 方向决策）。
