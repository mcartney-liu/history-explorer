# Historical Milestone Archive

Target location for M1–M8.6 milestone reports.

**Status:** migration PENDING (separate future task — not part of M8.8-C).
Current M* reports still live at `docs/` root (e.g. `M3-001_Architecture.md`, `M3.5-*`, `M4-*`, `M5-*`).

Once migrated, each milestone gets its own subfolder (`M1/`, `M2/`, … `M8.6/`) plus an `INDEX.md`.
