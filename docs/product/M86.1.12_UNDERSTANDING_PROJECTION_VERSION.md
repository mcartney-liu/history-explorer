# M86.1.12 — Understanding Projection Version Contract
### Strict Read-Only / Version Model Freeze

> 模式声明：严格只读。冻结 UnderstandingProjection 的版本语义。不写代码。

---

## 修正：Projection 必须携带版本信息

UnderstandingProjection 新增 `basedOn.{ templateRef, templateVersion, projectionVersion }`。UnderstandingTemplate 新增 `version` 字段。

---

## 版本语义规则

1. Template 更新不修改历史 Projection
2. 同一 Exploration 内 Template 版本不变
3. Memory 保存时必须包含版本信息
4. projectionVersion 独立于 templateVersion（Projection 模型本身的版本）

---

## Memory 保存格式

```
{ explorationId, userQuestion, projection: { stage, coverageRatio, basedOn: { templateRef, templateVersion, projectionVersion, computedAt } } }
```

---

## 后续

```
M86.1.12 Version Contract ← 当前完成 → M86.1 Batch 3 ⬜
```
