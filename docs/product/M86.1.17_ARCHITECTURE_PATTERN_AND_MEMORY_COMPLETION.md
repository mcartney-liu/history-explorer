# M86.1.17 — Architecture Pattern & Memory Completion
### Strict Read-Only / Final Architecture Freeze

> 模式声明：严格只读。冻结 Projection Delta / Session Boundary / Memory Policy / BEDAE Pattern。不写代码。M86.1 最终收口。

---

## 修正 1：Memory Trigger = ProjectionDelta

ProjectionDelta { stageChanged?, coverageChanged?, dimensionsCompleted?, relationsCompleted?, missingLinksResolved? }。Memory Policy 判定哪些 Delta 进入 Memory。

## 修正 2：GrowthEvent 引用 Delta

GrowthEvent { deltaRef, sessionRef, decisionRef }。不直接保存 previous/new。

## 修正 3：latestProjection → latestSessionRef

Memory 保存 Session 引用。Projection 需要时从 Session + Assembler 生成。

## 修正 4：GrowthTimeline → GrowthGraph

Graph 结构（nodes + edges）。Timeline = Graph 的线性 View。

## 修正 5：Session Boundary

一个 Session = 一次 Exploration 生命周期。同一 explorationId 内所有 compute 属同一 Session。

## 修正 6：BEDAE Pattern

> Builder → Evaluator → Decision → Assembler → Explainability

统一 Runtime 架构模式。适用于 Understanding / Memory / Context / Experience / AI Companion。

---

## 后续

```
M86.1.17 Final Freeze ← 当前完成 → M86.2 ⬜
```
