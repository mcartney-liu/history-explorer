# M86.1.7 — Semantic Anchor Runtime Validation
### Strict Read-Only / Batch 2 Acceptance Test

> 模式声明：严格只读。用升级后的 Anchor/Relation 模型重走完整探索路径，验证 Batch 2 数据模型在真实运行链路中可行。不写代码。

---

## 场景

同一探索路径：Curiosity → Orientation → Discovery（罗马军团）→ Connection（罗马道路系统）→ Connection（罗马行省治理）。

---

## 阶段 1：Context 创建

anchorChain = [], relationChain = []。user_question/goal 来自 Curator 预写。✅

---

## 阶段 2：Discovery（罗马军团）

currentAnchor = { entityId: "roman-legion", entityProvenance: { source: "knowledge_layer" }, selectionContext: { source: "curator_layer", reason: "探索包入口" } }。anchorChain = [1 元素]，relationChain = []。✅ entityProvenance 始终 knowledge_layer，selectionContext 正确区分 curator_layer。

---

## 阶段 3：Connection（军团 → 道路）

currentAnchor.selectionContext = { source: "knowledge_layer", reason: "Related Entity: 罗马军团 → 罗马道路系统" }。anchorChain = [2 元素]，relationChain = []。✅ activeRelation = null（Causal Layer 未接入），不创建 Relation。

---

## 阶段 4：Connection（道路 → 治理）

anchorChain = [3 元素]，完整锚点序列形成。relationChain = []。✅

---

## 阶段 5：边界验证

| 边界 | 验证 | 状态 |
|---|---|---|
| entityProvenance 不被 selectionContext 污染 | 3 个 Anchor 的 entityProvenance.source 全为 knowledge_layer | ✅ |
| selectionContext 正确区分进入路径 | curator_layer → knowledge_layer → knowledge_layer | ✅ |
| relationChain 不与 ExplorerPath 混淆 | 行为记录 ≠ 语义记录 | ✅ |
| activeRelation=null 时不创建 Relation | relationChain 保持空 | ✅ |
| cognitiveStage 不由 Navigation 写入 | 始终 FACT | ✅ |

---

## TEMP-ANCHOR-001

guessEntityType() 返回硬编码 "concept"。M86.3 前必须替换为 Knowledge Layer 查询。不影响 Batch 2（entityType 暂不作为计算依据）。

---

## 结论

Batch 2 数据模型在真实运行链路中可行。M86.1 正式关闭。可以安全进入 Batch 3。
