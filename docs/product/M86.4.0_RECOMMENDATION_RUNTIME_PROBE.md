# M86.4.0 — Recommendation Runtime Probe Boundary Contract
### Strict Read-Only / Probe Freeze

> 模式声明：严格只读。冻结 M86.4 Probe 边界。不写代码。验证完成后移除 Probe。

---

## 定位

不是开发推荐系统。是用第三个 Domain Module 验证 Runtime 通用性。

## 一、Recommendation 是 Domain Module

RecommendationPolicy 与 UnderstandingPolicy/MemoryPolicy 同层。

## 二、输入

RecommendationContext { currentUnderstandingProjection, currentMemoryProjection, explorationGoal, availableKnowledgeSpace }。

## 三、输出

Decision<RecommendationPayload> { actionType, targetRef, reason, confidence }。

## 四、AI 不参与 Decision

RecommendationPolicy → Decision → LLM Explanation（可选）。LLM 解释"为什么"，不决定"推荐什么"。

## 实施计划

M86.4.0 → M86.4.1 RecommendationPolicy → M86.4.2 → M86.4.3 → M86.4.4 Remove Probe

---

## 后续

```
M86.4.0 Boundary ← 当前完成 → M86.4.1 ⬜
```
