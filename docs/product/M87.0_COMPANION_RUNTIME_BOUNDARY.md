# M87.0 — Companion Runtime Boundary Contract
### Strict Read-Only / Companion Boundary Freeze

> 模式声明：严格只读。冻结 M87 AI Companion 的 Runtime 边界。不写代码。

---

## 一、Companion = 第四个 Domain Module

CompanionPolicy 与 UnderstandingPolicy/MemoryPolicy 同层。

## 二、ConversationContext

{ sessionRef, currentTopic, userIntent, lastDecisionRef, activeExplorationRef, currentWorkspaceState }。短期交互上下文，非长期 Memory。

## 三、CompanionResponsePayload

Decision<CompanionResponsePayload> { actionType, targetRef, structuredContent, suggestedNextStep, confidence }。

## 四、AI/Decision 边界

Policy 决定 → LLM 表达。LLM 不判定意图/策略/事实。

## 五、LLM = Explanation Renderer

CompanionPolicy → Decision → Explanation Projection → LLM Renderer → Workspace。LLM 不在 Policy 内部。

## 六、Companion 消费 Memory

MemoryProjection → CompanionContextBuilder → CompanionPolicy。不操作 MemoryStore。

## 七、Replay/Explainability

Decision<T> / RuleTrace / Replay / Explainability 全部复用。

## 实施计划

M87.0 → M87.1 CompanionPolicy → M87.2 → M87.3 → M87.4

---

## 后续

```
M87.0 Boundary ← 当前完成 → M87.1 ⬜
```
