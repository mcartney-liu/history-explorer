# M86.2.1 — Understanding Memory Model Contract
### Strict Read-Only / Memory Model Freeze

> 模式声明：严格只读。冻结 Memory 核心数据模型。不写代码。

---

## 修正 1：移除 latestProjectionRef

唯一 Source = latestSessionRef。Projection 需要时从 Session 生成。

## 生命周期

创建(第一次 Delta.persist=true) → 更新(追加 GrowthEvent) → 完成(stage=UNDERSTANDING+结束) → 归档(30天无活动)

## Identity = Topic Identity

同一 topicRef → 同一 MemoryUnit。不同 topicRef → 不同 Unit。

## Merge Protocol

走 MemoryPolicy → MemoryDecision。M86.2 仅支持同一 topicRef 自动合并。

## Memory ↔ Exploration

一对多：sourceExplorationRefs[]。

## 最终模型

UnderstandingMemoryUnit { unitId, topicRef, userQuestion, latestSessionRef, sourceExplorationRefs[], growthGraph, versions, status }

---

## 后续

```
M86.2.1 Model ← 当前完成 → M86.2.2 ⬜
```
