# M86.3.0 — Runtime Stabilization Boundary Contract
### Strict Read-Only / Stabilization Freeze

> 模式声明：严格只读。冻结 M86.3 稳定化边界。不写代码。

---

## 一、App.tsx 拆分

目标：App = ApplicationCompositionRoot。App 不包含 Domain Logic / Delta 计算 / Policy 执行。

结构：runtime/evaluation/ + modules/understanding/ + modules/memory/ + app/AppCompositionRoot.tsx。

## 二、MemoryStore Contract

MemoryStore { saveMemoryUnit, loadMemoryUnit, appendGrowthNode, appendDecision, loadGrowthGraph }。Persistence 永远最后。MemoryPolicy 不访问 Store。

## 三、Replay Validation

Replay(oldDecisionPackage) === oldDecision。Policy 升级后 Replay 返回旧 Decision，ReEvaluate 返回新 Decision。

## 四、Test Harness

Decision Contract / Policy Protocol / Explainability / Replay / Version Compatibility 五项测试。

---

## 后续

```
M86.3.0 Boundary ← 当前完成 → M86.3.1 ⬜
```
