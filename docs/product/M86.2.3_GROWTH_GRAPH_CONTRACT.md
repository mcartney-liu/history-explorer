# M86.2.3 — Growth Graph Contract
### Strict Read-Only / Growth Graph Freeze

> 模式声明：严格只读。冻结 GrowthGraph 语义模型。不写代码。

---

## 修正 1：ProjectionDelta 增加 DeltaCause

user_progress / policy_change / template_change / reevaluation。非 user_progress 不追加 GrowthNode。

## 修正 2：GrowthNode 6 种类型

exploration_start / delta / milestone / branch / merge / reactivation。含 GrowthSnapshot { stage, coverageRatio, missingLinkCount, dimensionCount }。

## 修正 3：GrowthEdge 4 种类型

continues / branches / merges / references。仅同一 MemoryUnit 内。跨 Unit 留给 M87+。

## 修正 4：MemoryUnit 与 GrowthGraph 分离

growthGraphRef 引用独立 GrowthGraph。便于未来跨 Unit 关联。

## 修正 5：操作规则

Append Only。不修改、不删除、M86.2 不支持遗忘/跨 Unit 合并。

---

## 后续

```
M86.2.3 GrowthGraph ← 当前完成 → M86.2.4 ⬜
```
