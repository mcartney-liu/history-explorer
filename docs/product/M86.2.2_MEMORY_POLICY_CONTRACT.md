# M86.2.2 — Memory Policy Contract
### Strict Read-Only / Memory Policy Freeze

> 模式声明：严格只读。冻结 Memory Policy 判定规则。不写代码。

---

## 修正 1：MemoryDecision = Decision<MemoryPersistencePayload>

非第二套 Decision。继承 M86.1 Decision 模型（RuleTrace / evaluatorId / evaluatorVersion / inputRef）。

## 修正 2：MemoryPolicy 纯函数

evaluate(delta, existingState, context) → Decision。不可访问 Database/Session/Workspace。

## 修正 3：判定规则

P0: stage FACT→CONNECTION / CONNECTION→UNDERSTANDING / reactivation。
P1: coverage+0.2 / 新维度 / missingLinks-50%。
P2: relationsCompleted / stage 回退。
无变化不写入。同 Session 去重。

## 修正 4：reactivation 不覆盖历史

completed → 新增 GrowthNode(type='reactivation') + GrowthEdge → active。Append Only。

## 修正 5：Memory Explainability

Explain() → 结构化 Evidence（deltaType + ruleId + ruleTrace）。展示文本由 Experience Layer 翻译。

---

## 后续

```
M86.2.2 Policy ← 当前完成 → M86.2.3 ⬜
```
