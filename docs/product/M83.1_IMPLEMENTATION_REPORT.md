# M83.1 Implementation Report

> **阶段**：M83.1 Explorer Validation Instrumentation — Implementation
> **日期**：2026-08-05
> **基线**：M82 committed + M83.0 (pending)
> **依据**：M83.1 Instrumentation Design Gate Review + Implementation Boundary Review

---

## 1. Implementation Summary

| 维度 | 内容 |
| --- | --- |
| **任务** | M83.1 Explorer Validation Instrumentation |
| **变更文件** | 4 files（Frontend only） |
| **变更量** | +61/-6 lines |
| **新增测试** | 10（`m83.1_events.test.ts`） |
| **后端** | 0 files |
| **新依赖** | 0 |

---

## 2. Changes

### 2.1 UserBehaviorEvent.ts — Event Schema

```typescript
// 新增 5 个 BehaviorAction
| 'cs_card_view'
| 'cs_card_expand'
| 'cs_follow_entity'
| 'cs_guide_next'
| 'cs_evidence_open'

// 新增 1 个可选字段（无 causalConfidence）
interface UserBehaviorEvent {
  // ... 已有字段
  causalId?: string;  // M83.1 — from CausalStatement.id, not generated
}
```

### 2.2 CausalStatementCard.tsx — Event Emit

| 事件 | 触发时机 | 实现 |
| --- | --- | --- |
| `cs_card_view` | 组件 mount | `useEffect(() => recordEvent(...), [cs.id])` |
| `cs_card_expand` | Card 点击 | `onClick={handleCardClick}` → `recordEvent(...)` |
| `cs_evidence_open` | Evidence 按钮点击 | `onClick={e => { e.stopPropagation(); handleEvidenceClick(ref); }}` |

新增 props：`onCardClick?: () => void`（可选，不破坏现有调用者）。

### 2.3 GuidePanel.tsx — cs_guide_next

```typescript
// 在 CS reason 后的下一步点击中：
if (step.causal?.id) {
  recordEvent({ action: 'cs_guide_next', causalId: step.causal.id })
}
```

### 2.4 RelationshipChain.tsx — cs_follow_entity

```typescript
// 在 spine 实体点击中，当该边有 CS 时：
const edgeCS = edge && causalStatements
  ? resolveCausalForEdge(edge, causalStatements)
  : null

onClick: () => {
  if (edgeCS?.id) {
    recordEvent({ action: 'cs_follow_entity', causalId: edgeCS.id })
  }
  onEntityClick(gid)
}
```

同时简化了 CausalStatementCard 渲染（复用 `edgeCS` 变量，避免重复调用 `resolveCausalForEdge`）。

---

## 3. Event Matrix

| 事件 | 组件 | 触发条件 | causalId 来源 | 不触发条件 |
| --- | --- | --- | --- | --- |
| `cs_card_view` | CausalStatementCard | 组件 mount | `cs.id`（来自 CausalStatementData） | `cs.id` 为空 |
| `cs_card_expand` | CausalStatementCard | 用户点击 Card | `cs.id` | `cs.id` 为空 |
| `cs_follow_entity` | RelationshipChain | 用户点击有 CS 的边上的实体 | `edgeCS.id`（来自 resolveCausalForEdge） | 该边无 CS |
| `cs_guide_next` | GuidePanel | 用户点击 CS 理由后的下一步 | `step.causal.id`（来自 GuideSnapshot） | step 无 causal |
| `cs_evidence_open` | CausalStatementCard | 用户点击证据引用 | `cs.id` | `cs.id` 为空 |

---

## 4. Boundary Compliance

### 4.1 Governance Contract

| 约束 | 状态 |
| --- | --- |
| FU2：M83.1 — Validation Instrumentation | ✅ |
| Frontend only（不修改 backend） | ✅ |
| 不新增 AI Runtime | ✅ |
| 不修改 CausalStatement Schema | ✅ |
| 不修改 Graph Core | ✅ |
| 不修改推荐逻辑 | ✅ |

### 4.2 Implementation Boundary Review

| 边界 | 合规 |
| --- | --- |
| **Event Storage**：localStorage only | ✅ 复用现有 `recordEvent()` → localStorage 管道 |
| **causalId**：仅来自 `CausalStatement.id` | ✅ 不运行时生成（无 uuid/timestamp/hash） |
| **Component**：仅 emit 事件 | ✅ 无 UI 行为改变、无推荐逻辑改变、无 confidence 逻辑 |

### 4.3 Instrumentation Design Gate

| 事件 | 状态 |
| --- | --- |
| `cs_card_view` | ✅ |
| `cs_card_expand` | ✅ |
| `cs_follow_entity` | ✅ |
| `cs_guide_next` | ✅ |
| `cs_evidence_open` | ✅ |
| `causalConfidence` 字段 | ✅ 不存在 |
| `cs_confidence_notice` 事件 | ✅ 不存在 |

---

## 5. Test Results

### 5.1 New M83.1 Tests — 10/10 PASS

```
✓ recordEvent with causalId works for cs_card_view
✓ recordEvent with causalId works for cs_card_expand
✓ recordEvent with causalId works for cs_follow_entity
✓ recordEvent with causalId works for cs_guide_next
✓ recordEvent with causalId works for cs_evidence_open
✓ causalId is optional — not required for non-CS events
✓ causalId is present when provided
✓ no confidence field on event schema
✓ getEventsByAction filters CS events correctly
✓ existing non-CS events still work
```

### 5.2 Existing Tests — 55/56 PASS

1 FAIL = 已知假阳性（`data-confidence` HTML 属性被测试误判），M82 Release Readiness Review 中已裁定。非 M83.1 引入。

---

## 6. Scope Audit

```
git diff --stat:
  frontend/src/components/causal/CausalStatementCard.tsx  | 32 ++++++++++++++++-
  frontend/src/components/guide/GuidePanel.tsx             |  5 +++
  frontend/src/components/package/RelationshipChain.tsx    | 22 +++++++---
  frontend/src/data/UserBehaviorEvent.ts                   |  8 ++++
  (backend/app/core/exploration_engine.py                   | 10 +--  -- M83.0)

  5 files, +69/-8 (M83.0 + M83.1 合计)
  M83.1 only: 4 files, +61/-6
```

---

> M83.1 Implementation 完成。不 commit。等待 PO Review。
