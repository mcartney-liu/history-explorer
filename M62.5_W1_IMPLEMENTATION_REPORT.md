# M62.5 Stage A W1 Implementation Report

> Stage: W1 — Global Language Experience System Foundation
> Status: DONE (not committed)
> Baseline: HEAD `68cd0fa` (vM62), runtime 0.13.0, freeze-check PASS

## 1. Changed Files

新增：
- `frontend/src/lib/preferences.ts` — 统一偏好层（AppPreferences + 幂等迁移 + hook + getDisplayName 契约）
- `frontend/src/lib/format.ts` — Intl 日期/数字格式化
- `frontend/src/components/LanguageSwitcher.tsx` — 三语切换 UX
- `frontend/src/locales/index.ts` — lookup 引擎（exact → zh → key fallback）
- `frontend/src/locales/terminology.ts` — 9 类核心术语（zh/en/ja）
- `frontend/src/locales/zh/{app,common,discover,entity,relationship,timeline,workspace,ai,errors}.ts` (9)
- `frontend/src/locales/en/{app,common,discover,entity,relationship,timeline,workspace,ai,errors}.ts` (9)
- `frontend/src/locales/ja/{app,common,discover,entity,relationship,timeline,workspace,ai,errors}.ts` (9)

修改：
- `frontend/src/data/locale.tsx` — Locale 扩到 zh/en/ja；setLocale 委托 preferences；t() 改用 lookup
- `frontend/src/components/AppShell.tsx` — 内联 toggle 替换为 `<LanguageSwitcher />`

（W0 阶段已改 `scripts/freeze-check.mjs` 的 SCOPE_ALLOWLIST，不在 W1 新增范围）

## 2. Architecture Changes

- **Locale Engine**：`Locale` 类型 `zh|en` → `zh|en|ja`；`setLocale` 委托统一偏好层写 `he-prefs`；`t()` 改用 `lookup(locale→zh→key)` fallback 链（旧 `msg[key] ?? key` 无语言链，已升级）。
- **Preference Layer**：新增 `lib/preferences.ts`，`AppPreferences = { language, contentDisplayMode, properNameMode, aiPreference, future? }`，替代散落 `localStorage('he-locale')`；`he-locale`→`he-prefs.language` 幂等迁移（读旧→写新→删旧，重复安全）。
- **Resources**：`locales/` 模块化命名空间资源框架，zh/en/ja 三语 seed，key 强制 namespace 前缀（`common.button.ok` 风格）。
- **Switcher**：`LanguageSwitcher` 三语切换（SUPPORTED_LOCALES 数组驱动，加语言不改组件），无刷新生效，当前态高亮。

## 3. Freeze Result

```
freeze-check:  PASS
violations:    0
```

真实命令：`node scripts/freeze-check.mjs`（项目无 `freeze-check` npm script）。

## 4. Tests

- `npm test`（vitest run）：**105 files / 941 tests PASS** (exit 0) — 含既有 `AppShell.test.tsx`，证明 Switcher 替换无回归。
- `npm run build`（tsc && vite build）：exit 0（dist 产物生成）。注：首次因 vite `emptyDir` 撞 WorkBuddy safe-delete trash 拦截失败，`rm -rf dist` 后重跑通过（环境工具问题，非代码）。
- `tsc --noEmit`：0 error。

## 5. Scope Audit

| 项 | 状态 |
|---|---|
| backend 改动 | NONE ✅ |
| API contract | 未改动（aiClient.ts 未碰）✅ |
| dependency | 零新增 ✅ |
| AI language | 未实现（O5 仍 Out；仅 `aiPreference` 灰显契约）✅ |
| runtime version | 0.13.0 不变 ✅ |
| freeze logic | 未改 ✅ |

## 6. Remaining Work

- **Stage B**：全量组件文本外置（B1 实体/关系 → B2 核心壳层 → B3 实体页 → B4 发现页 → B5 工作台 → B6 AI 面板 → B7 长尾），强制只调 `t()`/`getTermLabel()`/`getDisplayName()`，禁硬编码。
- **P0.5 余下**：术语专业模式（LUX-03）/ 完整 Preference Center UI 属 P1 延后。
- **P2**：RTL / Lazy / extract-i18n / AI 管线 全缓。
- W1 成功标准 = "语言系统基础设施可运行"（**非**全站多语言化）—— 已达成。

## 7. Status

未 commit / 未 push / 未 tag（W1 禁止）。所有改动随 W11 发布 vM62.5 时一并 annotated commit + tag + push（仿 M62 流程）。
