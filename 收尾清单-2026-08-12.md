# 项目收尾清单（代码实证版 · 2026-08-12 更新）

> 全部结论以 git / 代码 / 测试实测为准，不凭记忆。本次已实际执行一批清理，见第一节。

## 一、本次已完成的清理（已落盘，未提交）

| 项 | 动作 | 验证 |
|----|------|------|
| PROJECT_ROADMAP.md | `git mv` 归档至 `docs/archive/` | git status 显示 R |
| OPEN-DECISIONS 顶部汇总行 | 更正：OD-08/09/10 已于 2026-08-08 Wave2 闭环 RESOLVED，自"仍 OPEN"移除 | 读详情行确认 RESOLVED |
| TopicStoryOpening.tsx | 移出 `src`（小梦遗留半成品，无任何 import 引用，未挂载） | grep 全仓无引用；src 下已不在 |
| conftest 重复 | `backend/tests/conftest.py` 改名 `_conftest_duplicate_disabled.py`（pytest 不再收集，根 conftest 仍覆盖全部） | 后端测试 **445 passed** |

## 二、M74 真实状态（代码实证）

- **"无法连接后端"横幅误显**：`App.tsx:497-531` 已带 retry-once 防误报（仅真·`/topics` 两次都失败才显示），**已修**。
- **search 400 误显**：`App.tsx:889-902` 已把 400 正确归类为"请求未通过校验"，不再误显"无法连接后端"，**已修**。
- **中文 chips 点击 400**：后端 `/search`（`main.py:186`）对中文 query 不校验、不返 400；`resolveEntryQuery` 对未知中文永不抛异常，解析不出则落 `handleSearch` → `/search` 返回结果/空。**当前代码下理论上不复现**，应为 M74 修复前的旧报告。若仍见，需实测运行中的后端版本（可能跑的是旧进程）。

> 结论：M74 两类问题在代码层均已修复/不复现，**无需改代码**。

## 三、剩余真实待办（按优先级）

### 🔴 需 PO 拍板
- **OD-04**：旧文档（PROJECT_CONTEXT / Product_DNA / PRD）同步到 Article 0 定位。等你定方向我出同步 PR。

### 🟠 需 PO 签字（不阻塞但收尾动作）
- **M81a**：20 份体验验证文档已提交（`git ls-files docs/product/M81a_*` = 20），待 Release 收口签字。

### 🟡 可选小修（非阻塞）
- **App.tsx:1406** `onQuickStart` handler 的 `package` 分支缺 `return`，解析为 package 时会同时触发多余 `search`。改动极小，且 `App.tsx` 已在冻结白名单（freeze-check 第 248 行），可随时补。

### ⚪ 未来里程碑（不阻塞发版）
- OD-01 / OD-03 / OD-05、R2/R3/R4/R7/R8：等后续里程碑或独立立项。

## 四、健康度（实测）

- 前端 `tsc --noEmit`：**0 错误**（M61 零错误仍成立）
- 后端 `pytest`：**445 passed**
- 技术债：仅 5 处前瞻 TODO（M85.8+ / D-2+），不阻塞
- `master..HEAD`：空（无漏合回代码）

## 五、未提交改动（待你决定是否提交 / 提 PR）

```
R  PROJECT_ROADMAP.md -> docs/archive/PROJECT_ROADMAP.md
M  docs/15_DECISIONS/OPEN-DECISIONS.md
R  backend/tests/conftest.py -> backend/tests/_conftest_duplicate_disabled.py
?? 收尾清单-2026-08-12.md   （本文件，未跟踪）
```

> 按 Release 铁律，提交/推送/打 tag 永远由你（翔哥）拍板。我做完清理不改 git 远端。
