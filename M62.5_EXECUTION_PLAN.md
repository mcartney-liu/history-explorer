# M62.5 Execution Plan — Global Language Experience System
(i18n Architecture + Localization + Terminology + Language UX + Preference Foundation)

> 状态：**Revision 2 — UX Enhancement Integration（PO 批准方案 B）**
> 基线：vM62（已发布，2026-07-29）｜ runtime 0.13.0 ｜ ENTITY_TYPES=8 ｜ RELATIONSHIP_TYPES=18
> 优先级：M62.5 成为 Trust Layer（M63）之前的最高 UX 优先级
> 约束：零后端改动 / 零 API 改动 / 零 schema 改动 / 零 dependency 改动（除非绝对必要）
> 本文件为规划定稿（Revision 2），**不含任何实现代码**

---

## 0. Execution Log（只读分析 + 规划修订）

| 阶段 | 任务 | 结果 | 证据 |
|---|---|---|---|
| R0 | Git / 冻结基线确认 | HEAD=vM62 (`68cd0fa`)，工作树仅剩排除项 | git status / git log |
| R1 | 现有 i18n 审计 | 自定义轻量 i18n 已存在（M60 `locale.tsx`），但采用率极低（全库仅 1 处 `t()`） | Explore 代理 + Grep |
| R2 | 硬编码串审计 | ~147 个 `.tsx` 几乎全硬编码中文；`entityLabels.ts` 仅中文；`aiClient.ts` 无 language 字段 | Explore 代理 + Read |
| R3 | 冻结边界确认 | 默认全冻结；新建 `locales/` 需 Freeze Revision Gate | Read `scripts/freeze-check.mjs` |
| R4 | 三核心文件精读 | `locale.tsx` / `entityLabels.ts` / `aiClient.ts` 逐行确认 | 直接 Read |
| R5 | Revision 1 Review（只读） | 产出 9 项修订建议，评级 A- / REVISE REQUIRED（轻量） | 《M62.5 Execution Plan Revision 1 Review》 |
| R6 | Final Planning Revision | 吸收 R1–R8 + 定稿 3 阻断项，计划升级为 Global Language System | Final Version（前版） |
| R7 | **M62.5 UX Enhancement Review（只读）** | 评估是否达 Museum Grade；识别 6 缺口（G1–G6 = LUX-01~05 + 统一偏好层）；结论 **B：轻量规划扩展后 Implementation** | 《M62.5 UX Enhancement Review》 |
| R8 | **Revision 2 Integration（本阶段）** | PO 批准 B；合入 M62.5A Language UX System + M62.5B Preference Foundation；新增 P0.5 层、Stage A/B/C Roadmap、AC-LUX-01~05、Freeze 显式条目 | 本文档重写 |

> ⚠️ 本文件仅为规划。未做：任何源码修改 / commit / push / tag / 真实 Freeze Gate 修改 / 实现代码。W0 Gate 对 `scripts/freeze-check.mjs` 的实际 allowlist 改动，在 Implementation 启动后的 W0 阶段执行；本节 §4.3 仅**规划**该增量。

---

## 1. M62.5 PRD（产品需求文档）

### 1.1 Background（背景）

vM60 声称"i18n support"，但实测只是 M60 落了一个**骨架**：`LocaleProvider` / `useLocale()` / `t()` + 一个双语字典 + 顶栏语言切换按钮。全代码库 `t()` 仅被调用 **1 次**（`AppShell.tsx:37`）。其余 ~147 个组件的用户可见字符串全部硬编码（中文为主，部分英文混用）。

**三个层面的缺失（原规划已识别）**：
1. **Internationalization（架构）**：无完整资源体系，字符串散落组件，切换语言只改顶栏 tagline。
2. **Localization（本地化）**：无集中日期/数字格式化；实体类型标签仅中文；关系解释句硬编码英文，与中文 UI 混排。
3. **Terminology（术语）**：核心领域概念无统一管理，译法随组件漂移。

**Revision 2 新增识别（R7 审查）**：即便补齐上述三层，原规划仍停在 **Translation Architecture** 级，未覆盖 **Language UX** 与 **统一偏好层**：
4. **Language UX（体验）**：语言切换器 UX 缺位（机制有、设计无）；UI 语言与内容/专名语言未解耦为双轴；专名显示策略未形式化；AI 语言偏好无 UX 契约。
5. **Preference Fragmentation（散落）**：全项目仅 `localStorage('he-locale')`，无统一偏好层，未来 `he-theme`/`he-setting` 必散落。

### 1.2 Executive Summary（执行摘要）

M62.5 从「名义国际化」升级为 **Global Language Experience System（全球语言体验系统）**——五根支柱：

- **Pillar A — Internationalization**：可插拔 `Locale` 注册表 + 模块化资源文件 + 全组件 `t(key)` 消费，运行时无刷新切换。
- **Pillar B — Localization**：实体/关系标签本地化、日期/数字按 locale 格式化、专名按显示策略呈现。
- **Pillar C — Terminology Management**：核心领域术语集中管理，跨模块一致、跨语言唯一，与普通 UI 文本分离。
- **Pillar D — Language UX（M62.5A，新增）**：三语切换器 UX、UI 语言 / 内容显示双轴模型、专名显示策略形式化、AI 语言偏好 UX 契约预留。
- **Pillar E — Preference Foundation（M62.5B，新增）**：统一 `PreferenceContext` + `he-prefs` 单一 store，根除散落 localStorage；当前仅实现语言类偏好，预留 Theme/Accessibility/Motion 槽位。

五支柱共同支撑"可信 AI 伙伴"（M63）之前的 UX 地基——没有它，多语言用户面前的 Trust Layer 仍是单语断肢。

### 1.3 Objectives

| # | 用户目标 | M62.5 交付定义 |
|---|---|---|
| O1 | 完整 i18n 架构（非仅中/英） | 可插拔 `Locale` 注册表 + 按语言/模块拆分的资源文件；**首发支持 ≥3 种语言**（zh / en / ja）证明架构可扩展，而非两套写死 |
| O2 | 运行时切换无刷新 | 复用现有 Context 状态机制，扩展为全量组件消费 `t()` |
| O3 | 全部用户可见 UI 串外置 | 建立 `t(key)` 单一真源 + Translation Key Convention；组件零硬编码可见串（专有名词/数据值除外） |
| O4 | 实体名语言感知 | 实体**类型**标签本地化（`人物↔Person`）；实体**专名**按 Proper Name Display Policy 呈现（Original/Localized/Dual）；原语言始终可回退 |
| O5 | AI 回复语言 | ⚠️ **架构缺口，移出 M62.5 范围**（见 §4.4 + §1.4 边界）。零 API 改动下不可达 |
| O6 | 日期/数字/格式本地化 | 新建 `lib/format.ts`，基于 `Intl.*` 按 locale 格式化；替换散落 ad-hoc 写法 |
| O7 | 冻结安全 | 零后端/API/schema/dependency 改动（依赖保持零 i18n 库，复用原生 `Intl`） |
| O8 | **AI 可翻译架构目标** | 所有用户可见文本通过稳定 key 契约 + 可序列化资源，支持未来 AI 翻译管线自动生产目标语言资源，而无需修改任何组件代码（纯前端可达） |
| **LUX-01** | **语言切换器 UX** | 三语 zh/en/ja 一键切换、**无刷新生效**、当前语言态明确、偏好持久化；新增语言**不改组件** |
| **LUX-02** | **双语言偏好模型** | **Axis 1 UI Language**（中文/English/日本語 UI）+ **Axis 2 Content/Proper-Name Display Mode**（Original/Localized/Dual）解耦为两个独立偏好轴 |
| **LUX-04** | **专名显示策略形式化** | 将 Entity Display Policy 升格为 **Proper Name Display Policy**，明确「专有名词 / UI 标签 / 领域术语」三者边界 |
| **LUX-05** | **AI 输出偏好契约** | 不实现 AI 回复语言（O5 仍 Out）；但预留 **AI Output Preference**（Follow UI / Original / Auto Detect）前端契约，灰显「Future Capability / Reserved Contract」，**零 API 改动** |
| **PF-01** | **统一偏好层** | `PreferenceContext` + `he-prefs` 单一 store 取代散落 `localStorage`；当前仅实现语言类偏好，Theme/Accessibility/Motion 仅预留 schema |

### 1.4 Scope（范围）

- **In Scope**：
  - Pillar A/B/C（原规划）：i18n 引擎增强、模块化资源体系、Translation Key Convention、Terminology Layer、Locale Strategy（含 Auto）、RTL Readiness 布局规范、全组件字符串外置、实体/关系标签本地化、日期/数字格式化。
  - **M62.5A（新增）**：LanguageSwitcher 组件（LUX-01）、双语言偏好轴（LUX-02）、Proper Name Display Policy（LUX-04）、AI Output Preference 契约（LUX-05，灰显）。
  - **M62.5B（新增）**：PreferenceContext / PreferenceService 架构 + `he-prefs` 统一 store + `he-locale` 向后兼容迁移；Theme/Accessibility/Motion 仅 schema 预留。
  - 语言切换 UX、测试迁移。
- **Out of Scope（默认）**：后端/API/schema/枚举/新依赖；**AI 回复内容翻译（O5，移出范围）**；真实多语言翻译内容生产（仅建架构 + 占位/机器可读骨架）；**Terminology Professional Mode（LUX-03，P1 延后）**；**完整 Preference Center UI（Theme/Accessibility/Motion 实现，P1 延后）**；RTL 实现 / Lazy Load / AI 翻译管线（P2）。
- **边界声明（O5 vs O8，防混淆）**：
  - **O5** = "AI *生成的回复* 内容语言跟随 UI"——受后端 `/api/v1/ai/explain` 不接收语言参数限制，零 API 改动不可达，记为已知架构缺口。LUX-05 仅为前端 UX 契约预留，不触碰此边界。
  - **O8** = "UI *资源*（locales 的 key + 源语言文本）可被外部 AI 翻译管线消费并生产目标语言资源"——纯前端可达，是长期架构目标，不影响发布。
  - 二者性质不同：**O5 是运行时 AI 输出，O8 是构建期/管线期资源生产**。实现者不得混淆。

### 1.5 用户价值

把"名义国际化"升级为"真实可切换、术语一致、格式本地化、UI/内容双轴可控、偏好统一、面向 AI 翻译的多语言体验产品"，是 Trust Layer 之前必须补齐的 UX 地基，并达 **Museum Grade UX** 标准。

---

## 2. Technical Architecture（技术架构）

### 2.1 核心原则：零依赖、扩展式、冻结安全
沿用 M60 的**零依赖自定义 i18n**（不引入 react-i18next / react-intl，避免 dependency 变更）。以原生 `Intl` 做格式化。PreferenceContext 用原生 React Context + JSON，不引入状态库。

### 2.2 资源目录结构（模块化）

```
frontend/src/locales/
├── terminology.ts            [术语层：跨语言概念定义 + 显示标签，见 §2.6]
├── zh/{app,common,discover,entity,relationship,timeline,workspace,ai,errors}.ts
├── en/  [同上 9 文件]
└── ja/  [同上 9 文件]
```

**为什么采用模块拆分**：可维护性（改 discover 不碰 entity）、Tree Shaking、多人翻译协作、缩小 Merge Conflict 面、保持每文件可读（预计 50–200 行）。

### 2.3 资源加载策略
- `LocaleProvider` 初始化时按 `SUPPORTED_LOCALES` 加载对应 `locales/<lang>/*.ts` 并合并为运行时 `MESSAGES` 表；`t(key)` 在运行时表查当前 locale。
- **当前（M62.5）**：3 种语言、体积小，**全量加载**（简单、首屏无感）。
- **未来 lazy load**：因资源已按「语言/模块」物理拆分，天然支持动态 `import()`。架构已铺路。
- 资源用 `.ts`（类型安全 + 零 loader）；同时提供构建期导出 JSON 快照供 O8 翻译管线（见 §4.4③）。

### 2.4 运行时切换（无刷新）+ PreferenceContext 包裹
- 沿用 `localStorage` + React state。切换时仅 `setState` → Context 变化 → 所有 `useLocale()` / `usePreferences()` 消费组件重渲染。**无需路由/刷新**。
- **Revision 2 调整**：`PreferenceProvider` 作为外层 Context 包裹 `LocaleProvider`（或二者合并为一棵偏好树）。`UI Language`（Axis 1）写入 `prefs.language` → 驱动 `t()` 与 `getTermLabel()` 的 locale 解析；`Content Display Mode`（Axis 2）写入 `prefs.contentMode` → 驱动 `getDisplayName()` 的呈现策略。
- `main.tsx:13` 已包裹 `LocaleProvider` 全树；Implementation 时升级为 `PreferenceProvider`。

### 2.5 Translation Key Convention（翻译键规范）

所有 UI 文本 key **必须带命名空间前缀**，禁止裸 key。命名空间与 §2.2 模块一一对应：

| 命名空间 | 覆盖 | 示例 |
|---|---|---|
| `common.*` | 通用按钮/状态/加载/空态 | `common.loading`, `common.confirm` |
| `app.*` | 应用壳层/品牌/导航/切换 | `app.switch_lang`, `app.tagline` |
| `discover.*` | 发现页 | `discover.explore`, `discover.recommend` |
| `entity.*` | 实体类型/字段 | `entity.type.Person`, `entity.field.birth` |
| `relationship.*` | 关系类型/解释 | `relationship.type.Influenced` |
| `timeline.*` | 时间线 | `timeline.axis`, `timeline.event` |
| `workspace.*` | 工作台 | `workspace.panel`, `workspace.action` |
| `trust.*` | 可信层（溯源/证据） | `trust.grounded`, `trust.partial` |
| `history.*` | 历史叙事 | `history.era`, `history.civilization` |
| `ai.*` | AI 面板 | `ai.explain`, `ai.ask` |
| `errors.*` | 错误 | `errors.network`, `errors.not_found` |

**规则**：
1. 新增任何 UI 文本 → 必须先加 `locales/<lang>/<module>.ts` 的 key，组件只引用 `t('namespace.key')`。
2. key 采用 `namespace.section.element` 三段式；同名概念跨模块复用同一命名空间，禁止复制。
3. `common.*` 仅放真正通用的原子串，禁止把模块特有串塞进 common（防垃圾桶）。

### 2.6 Terminology Layer（术语层）

核心领域术语是产品概念骨架，须**独立管理、跨模块唯一**。

**位置**：`frontend/src/locales/terminology.ts`（落在已放行 `locales/` 前缀下，零新 Gate 风险）。

**结构**：
```ts
export interface TerminologyEntry {
  id: 'entity' | 'relationship' | 'journey' | 'timeline'
    | 'evidence' | 'trust' | 'story' | 'discovery' | 'history';
  definition: string;                         // 概念定义（源语言，开发参考）
  labels: Record<Locale, string>;             // 各语言显示标签
}
export const TERMINOLOGY: Record<string, TerminologyEntry> = {
  entity:      { id:'entity', definition:'...', labels:{ zh:'实体', en:'Entity', ja:'実体' } },
  relationship:{ id:'relationship', definition:'...', labels:{ zh:'关系', en:'Relationship', ja:'関係' } },
  journey:     { id:'journey', definition:'...', labels:{ zh:'探索旅程', en:'Journey', ja:'旅程' } },
  timeline:    { id:'timeline', definition:'...', labels:{ zh:'时间线', en:'Timeline', ja:'タイムライン' } },
  evidence:    { id:'evidence', definition:'...', labels:{ zh:'证据', en:'Evidence', ja:'証拠' } },
  trust:       { id:'trust', definition:'...', labels:{ zh:'可信', en:'Trust',"... } },
  story:       { id:'story', definition:'...', labels:{ zh:'叙事', en:'Story', ja:'物語' } },
  discovery:   { id:'discovery', definition:'...', labels:{ zh:'发现', en:'Discovery', ja:'発見' } },
  history:     { id:'history', definition:'...', labels:{ zh:'历史', en:'History', ja:'歴史' } },
};
```

**术语层 vs 普通翻译层（正交）**：
- 术语层 = 产品概念的稳定 ID + 定义 + 跨语言显示标签（集中在 `terminology.ts`）。
- 普通 UI 层 = 按钮/提示等，走 `t('namespace.key')`，分散在 `locales/<lang>/*.ts`。
- 组件显示术语时调用 `getTermLabel(id, locale)`（读 `TERMINOLOGY[id].labels[locale]`），**不**把术语写进普通资源。
- 普通 UI 文本**不得**用术语词硬编码（如按钮写"查看实体"应引用术语标签，而非字面"实体"）。

### 2.7 Dual Language Preference Model（LUX-02，双语言偏好模型）★新增

M62.5A 核心：将"语言"拆为**两个独立偏好轴**，用户可分别控制。

| 轴 | 含义 | 取值 | 驱动 |
|---|---|---|---|
| **Axis 1 — UI Language** | 界面语言 | `zh` / `en` / `ja` / `auto` | `t()` 与 `getTermLabel()` 的 locale 解析 |
| **Axis 2 — Content / Proper-Name Display Mode** | 历史内容/专名呈现 | `original` / `localized` / `dual` | `getDisplayName()` 的呈现策略 |

**三者边界铁律（LUX-02 + LUX-04 共同约束）**：

| 类别 | 归属 | 处理方式 | 示例 |
|---|---|---|---|
| **Entity Name（专名，数据值）** | **数据层**（后端 `entity.name`） | 永不进 `t()`；由 `getDisplayName()` 按 Axis 2 呈现 | `孔子` / `Confucius` |
| **Entity Label（类型标签，UI）** | **UI 层** | 走 `t('entity.type.Person')` 本地化 | `人物` / `Person` |
| **Terminology（领域术语）** | **术语层** | 走 `getTermLabel(id, locale)` | `Entity` / `Relationship` / `Trust` |

- **严禁混用**：专名不得进 `t()`；UI 标签不得硬编码专名；术语不得写进普通资源。
- **解耦价值**：用户可设 `UI=English` 但 `Content Mode=Original`，即"英文界面 + 孔子原名的双轴体验"。

### 2.8 Proper Name Display Policy（LUX-04，专名显示策略）★升格

将原 §2.7 Entity Display Policy **升格为通用 Proper Name Display Policy**，覆盖所有专有名词（不限实体），并明确三者边界（见 §2.7 铁律表）。

**三种显示模式**（由 Axis 2 `contentMode` 控制，默认 `dual`）：
1. **Original**：仅显示源数据值，如 `孔子`。
2. **Localized**：显示本地化层（若数据层提供 `localized_name` 或前端映射表则显示，否则回退 Original），如 `Confucius`。
3. **Dual**：并排显示 Original + Localized，如 `孔子 / Confucius`（最安全，兼顾溯源与可读性）。

**解析函数**：`getDisplayName(value, { mode, localized? })` 统一处理 Proper Noun 的三模式呈现；UI 标签与术语分别走 `t()` / `getTermLabel()`，三者调用入口清晰分离。

- 本里程碑**不要求后端提供 `localized_name` 字段**，仅预留消费接口；无该字段时 Localized/Dual 自动回退 Original。
- 关系专名（如 actor 名）同理属数据值。

### 2.9 Locale Strategy（语言策略）

支持四选项（Axis 1 取值）：

| 选项 | 行为 |
|---|---|
| **Auto** | 读取 `navigator.language`，匹配 `SUPPORTED_LOCALES`（BCP47 前缀匹配，如 `zh-CN`→`zh`）；未匹配则 fallback |
| **中文** | 强制 `zh` |
| **English** | 强制 `en` |
| **日本語** | 强制 `ja` |

**Fallback 机制**：
- Auto 未匹配任何支持语言 → 回退到 `DEFAULT_LOCALE = 'zh'`。
- 某 key 在当前语言缺失 → `t()` 回退到 `DEFAULT_LOCALE` 对应文本；仍缺失 → 返回 key 本身（开发可见，不白屏）。
- 未来扩展：新增语言 = 在 `SUPPORTED_LOCALES` 加一项 + 加 `locales/<newlang>/` 目录 + （可选）O8 管线生成资源。组件零改。

### 2.10 Preference Foundation（M62.5B，统一偏好层）★新增

**目标**：根除散落 `localStorage('he-locale')` / `he-theme` / `he-setting` 陷阱，建立统一偏好边界。

**架构**：
```ts
// frontend/src/lib/preferences.ts（或 frontend/src/context/PreferenceContext.tsx）
export interface AppPreferences {
  language: Locale | 'auto';                 // Axis 1 — UI Language
  contentMode: 'original' | 'localized' | 'dual';   // Axis 2 — Content Display Mode
  terminologyMode: 'localized' | 'canonical';       // LUX-03 专业模式（P1 预留，当前仅 schema）
  aiPreference: 'follow-ui' | 'original' | 'auto-detect'; // LUX-05 预留，当前仅 schema
  future: {
    theme?: 'light' | 'dark' | 'system';     // 仅 schema 预留，不实现
    accessibility?: Record<string, unknown>; // 仅 schema 预留
    motion?: 'reduced' | 'full';             // 仅 schema 预留
  };
}
```

**统一存储**：单一 `localStorage('he-prefs')` JSON blob，由 `PreferenceContext` / `PreferenceService` 读写。**禁止**任何模块自行 `localStorage.setItem('he-*')`。

**当前实现范围**：仅 `language` + `contentMode` 实际生效（驱动 LUX-01/02/04）；`terminologyMode` / `aiPreference` / `future.*` **仅 schema 预留，无 UI、无行为**。

**向后兼容迁移（关键）**：
1. 初始化时若 `he-prefs` 不存在但 `he-locale` 存在 → 读取 `he-locale` 值写入 `prefs.language`，随后删除 `he-locale`。
2. 之后所有读取统一走 `he-prefs`。
3. 迁移为一次性、幂等；旧 key 清理不破坏已发布版本行为。

### 2.11 AI Output Preference Contract（LUX-05，AI 输出偏好契约）★新增

**原则**：不实现 AI 回复语言（O5 仍 Out Of Scope，零 API 改动）。仅在前端预留 **AI Output Preference** 契约：

| 选项 | 含义 |
|---|---|
| `follow-ui` | AI 输出跟随当前 UI Language（未来后端支持时生效） |
| `original` | AI 输出使用原文语言 |
| `auto-detect` | 自动检测 |

- 当前表现为**灰显 UX**（"Future Capability / Reserved Contract"），状态存入 `prefs.aiPreference`（§2.10 schema）。
- **严禁**向 `aiClient.ts` 请求体添加 `language` 字段（避免死代码，O5 边界保持）。
- 此契约确保未来后端支持时，UI 表面零改动即可接通。

### 2.12 格式化
- `lib/format.ts`：`Locale → BCP47` 映射（`zh→zh-CN`, `en→en-US`, `ja→ja-JP`）；`formatDate`/`formatNumber` 复用 `Intl`。替换 `ResearchLibrary.tsx:22` 的 `toLocaleDateString('zh-CN')` 硬编码。

### 2.13 实体名语言感知（收敛自原 §2.5，现归 Proper Name Policy）
- **类型标签**：`getEntityLabel(type, locale)` 返回本地化；未翻译回退 `zh` 或原始 type 字符串。
- **实体专名**：按 §2.8 Proper Name Display Policy 三模式呈现（默认 Dual），由 `getDisplayName()` 解析。
- **关系类型标签**：`understandingRules.ts` 中英文解释模板抽取为按语言模板（见 §7.3）。

### 2.14 RTL Readiness（RTL 预留）
- **当前不实现 RTL**（Arabic / Hebrew 不在 M62.5 范围）。
- **布局规范锁定**：使用 CSS logical properties（`margin-inline` / `padding-inline` / `text-align: start`）；flex/grid 自然流向；图标/装饰左右语义用逻辑方向或语义类名。未来支持 RTL 时仅需在根节点加 `dir="rtl"` + 少量覆盖样式。

---

## 3. Execution Roadmap（执行路线图，Stage A/B/C 重构）

> 全部纯前端组合，按"先引擎后外置、先核心后长尾"分批。每批配套测试迁移 + 门禁。

### Stage A — Language Foundation（语言地基）

| 批次 | 内容 | 涉及文件（代表性） | 门禁 |
|---|---|---|---|
| **W0** | Freeze Revision Gate：提 ADR + allowlist 增量（新增 `locales/`、`lib/format.ts`、顶层组件枚举、**LanguageSwitcher.tsx、PreferenceContext.tsx / lib/preferences.ts**；`terminology.ts` 已落 `locales/` 前缀无需单独加） | `scripts/freeze-check.mjs`（仅 allowlist 段） | freeze-check PASS |
| **W1** | i18n 引擎增强（`Locale` 注册表化、资源外置加载、`t()` 保持）+ **Preference Foundation Skeleton**（`PreferenceContext` + `he-prefs` schema + `he-locale` 迁移）+ **LanguageSwitcher**（LUX-01）+ **Key Convention 骨架** | `data/locale.tsx` + `locales/{terminology.ts, zh/en/ja/*}.ts` + `context/PreferenceContext.tsx` 或 `lib/preferences.ts` + `components/LanguageSwitcher.tsx` | emoji-scan / typecheck |
| **W2** | 格式化层：`lib/format.ts` + 替换散落日期/数字 | `lib/format.ts`、`ResearchLibrary.tsx` 等 | typecheck / unit |
| **W3** | 术语层接入：`getTermLabel` 替换硬编码概念词 | `terminology.ts` 消费点 | unit |
| **W4** | Proper Name Display Policy：`getDisplayName()` + Entity/Relationship 标签本地化 + Axis 2 接入 | `data/entity/entityLabels.ts`、`understandingRules.ts`、`lib/preferences.ts` | unit（locale-aware） |
| **Stage A Review** | 验收 P0 + P0.5 地基：三语引擎、PreferenceContext、LanguageSwitcher、双轴、Proper Name Policy、AI 契约预留、Freeze PASS | — | 门禁：AC-01~05, AC-LUX-01~05 |

### Stage B — Full Migration（全量迁移，固定顺序）

> **强制约束**：所有迁移组件**只能**调用 `t()` / `getTermLabel()` / `getDisplayName()`；**禁止**硬编码 UI 文本（专有名词/数据值除外，且须走 `getDisplayName()`）。

| 顺序 | 批次 | 内容 | 涉及文件（代表性） |
|---|---|---|---|
| B1 | 实体/关系 | Entity Label 本地化 + 关系解释模板 + 专名呈现 | `data/entity/entityLabels.ts`、`understandingRules.ts`、`components/entity/` |
| B2 | 核心壳层 | App / AppShell / nav / tab 外置 + LanguageSwitcher 集成 | `App.tsx`、`App.css`、`AppShell.tsx` |
| B3 | 实体页 | EntityHero / EntityHeader / RelationshipView | `components/entity/`、`RelationshipView.tsx` |
| B4 | 发现页 | DiscoverPage / 子组件 | `pages/DiscoverPage.tsx`、`components/discover/` |
| B5 | 工作台 | Workspace / Research* / HistorianChat | `components/workspace/`、`data/ai/` |
| B6 | AI 面板 | AIExplanationPanel / PROMPT_MODES（AI 文案外置 + AI 偏好契约灰显） | `components/ai/`、`data/ai/` |
| B7 | 长尾 | 顶层 `components/*.tsx` 未列者 | 随 W0 Gate 加入白名单者 |

每批完成跑 `freeze-check` + `emoji-scan` + `tsc` + `vitest`，全绿再下一批。

### Stage C — Release（发布）

| 批次 | 内容 | 门禁 |
|---|---|---|
| **W10** | 测试迁移：断言硬编码串的测试改为 locale-aware | `DiscoverPage.test.tsx`、`App.smoke.test.tsx`、`m62-entity-labels.test.ts` → vitest 全绿 |
| **W11** | 文档同步 + 发布（仿 M62）：README/PROJECT_CONTEXT/CHANGELOG → vM62.5，commit + tag + push + consistency 7/7 | 全门禁 8/8 |

---

## 4. Freeze Impact Analysis（冻结影响分析）

### 4.1 模式回顾
`scripts/freeze-check.mjs` 采用 **allowlist 模式**：`backend/` 与 `frontend/` 默认全冻结，仅命中 `SCOPE_ALLOWLIST` 精确条目或目录前缀才放行。

### 4.2 已放行（可直接改，无需新 Gate）
| 文件/目录 | 来源 Gate |
|---|---|
| `frontend/src/data/locale.tsx` | M60-021 |
| `frontend/src/data/entity/`（`entityLabels.ts`） | M59-005 |
| `frontend/src/data/ai/`（`aiClient.ts`） | M59-011 / M36.0 |
| `frontend/src/App.tsx` / `App.css` | M34-A |
| `frontend/src/pages/` | M35（目录前缀） |
| `frontend/src/components/ui/` `entity/` `workspace/` `ai/` `discover/` `exploration/` `journey/` | M59/M35（目录前缀） |
| `frontend/src/__tests__/` | M59+M60（目录前缀，测试随源改） |
| 大量精确列出的组件 | M36–M42 |

### 4.3 需 Freeze Revision Gate 新增（W0 必须先行）★Revision 2 更新

| 新增/缺口 | 类型 | 建议 allowlist 条目（**显式枚举**） |
|---|---|---|
| `frontend/src/locales/` | 新建目录 | `frontend/src/locales/`（目录前缀）— **含 `terminology.ts` 与 `zh/en/ja/*`** |
| `frontend/src/lib/format.ts` | 新建文件 | `frontend/src/lib/format.ts` |
| `frontend/src/components/LanguageSwitcher.tsx` | 新建顶层组件 | **精确条目**（并入 D4 枚举） |
| `frontend/src/context/PreferenceContext.tsx` **或** `frontend/src/lib/preferences.ts` | 新建文件 | **精确条目**（二选一，由 W0 定） |
| 顶层 `components/*.tsx` 未列者 | 未覆盖 | 枚举补入（**须 ADR 明确限定"仅 i18n 字符串外置，禁业务/API/枚举改动"**） |

> **⚠️ Least Privilege 铁律**：**禁止**以任何理由开放 `frontend/src/components/` 全目录前缀。所有新组件（LanguageSwitcher 等）必须**精确枚举**入 `SCOPE_ALLOWLIST`。 PreferenceContext 优先落 `lib/preferences.ts`（lib/ 前缀更窄、风险更低）；若采用 `context/` 则需为该单文件精确加条目。

> **冻结影响结论**：M62.5 纯前端组合，`backend/` 零触碰、零 schema/枚举/API/dependency 改动。**唯一必要动作 = 一次 Frontend Freeze Revision Gate（轻量 ADR + allowlist 增量）**，机制同 M30-A / M61。不引入任何新依赖。

### 4.4 ⚠️ 约束冲突与阻断定稿

**① O5 与 O8 区别（已定稿，防混淆）**
- **O5（AI 回复语言）**：零 API 改动下**不可达** → **移出 M62.5 范围，记为已知架构缺口**。LUX-05 仅为前端 UX 契约，不触碰此边界（不向 `aiClient.ts` 加字段）。
- **O8（UI 资源 AI 可翻译）**：纯前端可达，长期架构目标，不影响发布。

**② terminology.ts 最终位置（已定稿）**：`frontend/src/locales/terminology.ts`（落 `locales/` 前缀，零额外 Freeze 风险）。

**③ 资源格式兼容未来 AI 翻译（已定稿）**：源 `.ts` + 构建期 `scripts/extract-i18n.mjs` 导出 `locales/<lang>.json` 快照（key→源文），AI 管线消费 + `terminology.ts` 术语定义 → 生成目标语言资源 → 注册表加一项，**组件零改**。

---

## 5. Risk Assessment（风险评估）

| ID | 风险 | 等级 | 触发条件 | 缓解 |
|---|---|---|---|---|
| R1 | **Freeze 未放行即提交** | 🔴 High | 漏提 W0 Gate，新建 `locales/` 触发 D 类违规 | W0 先行；CI `freeze-check` 拦截 |
| R2 | **AI 语言不可达** | 🔴 High | O5 在零 API 改动下无法满足 | §4.4① 移出范围，LUX-05 仅契约 |
| R3 | **测试大规模断裂** | 🟠 Med | 测试断言硬编码中文串 | W10 统一迁移；先改测试再改源 |
| R4 | **字典键不一致** | 🟠 Med | 147 组件外置产生重/漏 key | §2.5 Key Convention 强制命名空间 |
| R5 | **运行时切换遗漏** | 🟠 Med | 组件直读 localStorage/常量非 `useLocale` | 全量 grep 硬编码串；t() 覆盖率检查 |
| R6 | **专有名词误译** | 🟡 Low | 把 `entity.name` 当 UI 串外置 | §2.7/§2.8 铁律：数据值走 `getDisplayName()` |
| R7 | **单文件超 300 行** | 🟡 Low | locale.tsx 膨胀 | 资源外置到 `locales/`，引擎保持精简 |
| R8 | **新增依赖诱惑** | 🟡 Low | 引 react-i18next | 约束零依赖；原生 `Intl` + Context |
| R9 | **资源体积（全量加载）** | 🟡 Low | 语言增多首屏涨 | §2.3 模块化已铺路，未来 lazy load |
| R10 | **术语与 UI 文本混用** | 🟠 Med | 概念词硬编码进普通资源 | §2.6 术语层独立 + `getTermLabel` 消费 |
| **R11** | **偏好迁移失败** | 🟠 Med | `he-locale`→`he-prefs` 迁移丢偏好 / 旧版本行为破坏 | §2.10 幂等一次性迁移 + 旧 key 清理 + 回归测试 |
| **R12** | **双轴概念混淆** | 🟠 Med | 开发者把 UI Language 与 Content Mode 混用，或专名误进 `t()` | §2.7 铁律表 + Code Review 门禁 + `getDisplayName()` 单一入口 |
| **R13** | **散落 localStorage 复发** | 🟡 Low | 新偏好绕过 `PreferenceContext` 直写 `he-*` | §2.10 禁令 + lint/门禁检查 `localStorage.setItem('he-` |

---

## 6. Acceptance Criteria（验收标准）

> EARS 格式，QA 以之为唯一测试依据。★Revision 2 新增 P0.5 层与 AC-LUX-01~05。

| 编号 | 功能 | EARS 验收标准 | 优先级 |
|---|---|---|---|
| AC-01 | 语言注册表 | While 系统启动，系统**必须**加载 `SUPPORTED_LOCALES` 全部语言资源 | P0 |
| AC-02 | 多语言 | 系统**必须**至少支持 zh / en / ja，证明非双写死 | P0 |
| AC-03 | 运行时切换 | When 用户切换语言，系统**必须**在不刷新下更新全部消费 `t()` 的组件 | P0 |
| AC-04 | 串外置 | While 渲染任意页面，全部用户可见 UI 串**必须**来自 `t(key)`（专有名词/数据值除外） | P0 |
| AC-05 | 实体类型标签 | If 当前语言为 en，系统**必须**将 `Person` 显示为 `Person`；无翻译回退原文 | P0 |
| AC-06 | 专名保留 | While 显示实体专名，系统**必须**按 Proper Name Display Policy 呈现（默认 Dual），不翻译数据值 | P0.5 |
| AC-07 | 日期格式化 | When 渲染日期，系统**应该**按 locale 通过 `Intl.DateTimeFormat` 输出 | P1 |
| AC-08 | 数字格式化 | When 渲染数字，系统**应该**按 locale 通过 `Intl.NumberFormat` 输出 | P2 |
| AC-09 | 关系解释本地化 | While 渲染关系解释，系统**必须**消除中英混排，按语言取模板 | P1 |
| AC-10 | 术语一致 | While 渲染核心概念词，系统**必须**通过 `getTermLabel(id, locale)` 取术语层，禁止硬编码概念词 | P1 |
| AC-11 | Key 规范 | When 新增 UI 文本，系统**必须**使用 `namespace.section.element` 命名空间 key | P1 |
| AC-12 | Auto Locale | When 用户选 Auto，系统**必须**按 `navigator.language` 匹配支持语言，否则 fallback 默认 | P1 |
| AC-13 | RTL 规范 | The codebase**必须**使用 CSS logical properties，保证未来 RTL 无需推翻 UI | P2 |
| AC-14 | 冻结安全 | 系统**必须**在 `freeze-check` 零 D 类违规（W0 Gate 后） | P0 |
| AC-15 | 零依赖 | 系统**必须**不新增任何 npm dependency | P0 |
| AC-16 | 测试全绿 | After 迁移，系统**必须**使 `vitest run` 全部通过 | P0 |
| AC-17 | O8 架构可达 | The system**应该**能通过 `extract-i18n` 导出 key+源文快照，供 AI 翻译管线消费 | P2 |
| **AC-LUX-01** | 语言切换 UX | When 用户切换语言，系统**必须**在不刷新下更新可见 UI，且 LanguageSwitcher **必须**明确显示当前语言态 | **P0.5** |
| **AC-LUX-02** | 双轴分离 | The system**必须**将 UI Language 与 Content Display Mode 作为两个独立偏好分别持久化与生效 | **P0.5** |
| **AC-LUX-03** | 专名三模式 | When 渲染专有名词，系统**必须**按 `original` / `localized` / `dual` 之一呈现（默认 `dual`），且 Entity Name / Label / Terminology 三者边界不被混淆 | **P0.5** |
| **AC-LUX-04** | 统一偏好层 | The system**必须**将所有用户偏好经统一 `PreferenceContext` / `he-prefs` 读写，**禁止**组件直写 `localStorage('he-*')` | **P0.5** |
| **AC-LUX-05** | AI 偏好契约 | The system**应该**在前端提供 AI Output Preference（Follow UI / Original / Auto Detect）契约并灰显为 Reserved，且**不得**向后端请求添加 language 字段 | **P0.5** |

---

## 7. Migration Strategy for Existing Components（存量组件迁移策略）

### 7.1 迁移三定律
1. **先测试后源码**：优先改断言硬编码串的测试（R3），locale-aware，避免改源后测试大规模红。
2. **数据值不外置**：`entity.name`、图谱数据值属数据，永不进 `t()`，须走 `getDisplayName()`。
3. **最小侵入**：每组件仅把可见串替换为 `t('key')` + 补 key；不改布局/逻辑。

### 7.2 分批迁移映射（代表性）
| 存量位置 | 现状 | 迁移动作 | 白名单状态 |
|---|---|---|---|
| `App.tsx` (688/694/735/741) | 硬编码中文 | `t('view.list')` 等 | 已列（M34） |
| `AppShell.tsx` | 硬编码 | `t('app.switch_lang')` + 集成 LanguageSwitcher | 已列 |
| `AIAction.ts` (32-87) | 硬编码中文 | `t('action.explain_entity')` | data/ai 前缀 |
| `DiscoverPage.tsx` | 硬编码 | 全量 `t()` | pages/ 前缀 |
| `entityLabels.ts` | 仅中文 | 扩为按语言 | data/entity 前缀 |
| `understandingRules.ts` | 英文模板 | 抽取为按语言模板 | 需 W0 核验 |
| `ResearchLibrary.tsx:22` | `toLocaleDateString('zh-CN')` | `formatDate(d, locale)` | **未列 → W0 补** |
| 顶层 `components/*.tsx` 未列者 | 硬编码 | `t()` | **未列 → W0 补** |

### 7.3 `understandingRules.ts` 特殊处理
模板生成英文关系解释，与中文 UI 混排。迁移：模板按语言抽取（zh/en/ja），由 `getExplanation(type, locale)` 选择。W0 核验白名单。

### 7.4 术语迁移
组件中所有硬编码概念词（"实体/关系/旅程/时间线/证据/可信/叙事/发现/历史"）替换为 `getTermLabel(id, locale)` 调用，源定义统一进 `terminology.ts`（W3）。

### 7.5 偏好迁移（★新增，M62.5B）
- 初始化逻辑：若 `he-prefs` 不存在且 `he-locale` 存在 → 读 `he-locale` 写入 `prefs.language` → 删除 `he-locale`。
- 所有既有/新增偏好统一经 `PreferenceContext` 读写；禁止任何模块 `localStorage.setItem('he-')`。
- `he-locale` 清理后做回归：确认旧版本已发布行为不被破坏。

### 7.6 测试迁移清单（必改）
- `pages/DiscoverPage.test.tsx`：硬编码中文断言 → locale-aware。
- `__tests__/App.smoke.test.tsx`：`'加载中'` → locale-aware。
- `__tests__/m62-entity-labels.test.ts`：中文标签断言 → `getEntityLabel(type, 'zh')` 显式。
- 其余断言 UI 串的测试同理。

### 7.7 回退与一致性
每批完成跑 `freeze-check` + `emoji-scan` + `tsc` + `vitest`，全绿再下一批。发布走 M62 同款流程。

---

## 8. Open Decisions for PO（待 PO 拍板项）

| # | 决策点 | 推荐项 | 备选 | 状态 |
|---|---|---|---|---|
| D1 | AI 回复语言（O5）是否进 M62.5 | **(A) 移出范围，记为架构缺口** | (B) 开后端 Gate / (C) 前端死代码 | 推荐 A（已采纳） |
| D2 | 第 3 语言 | **ja（日语）** | fr / 用户指定 | 推荐 ja |
| D3 | 资源文件格式 | **`.ts` 源 + JSON 快照派生** | 纯 `.json` + 动态 import | 推荐 .ts |
| D4 | 顶层组件白名单方式 | **显式枚举补入**（含 LanguageSwitcher.tsx） | 宽 `components/` 前缀（禁止） | 推荐枚举 |
| D5 | 是否现在推 W0 Gate | **先出计划待批，批准后再提 Gate** | — | 待批 |
| D6 | terminology.ts 位置 | **`frontend/src/locales/terminology.ts`** | `data/terminology.ts`（需新 Gate） | ✅ RESOLVED |
| **D7** | **PreferenceContext 落点** | **`frontend/src/lib/preferences.ts`（lib/ 前缀更窄）** | `frontend/src/context/PreferenceContext.tsx` | 推荐 lib/（W0 定） |
| **D8** | **LUX-03 术语专业模式** | **P1 延后（本地化术语已正确，开关为增强）** | 拉入 P0.5 | 推荐 P1 |
| **D9** | **偏好迁移策略** | **M62.5 内 `he-locale`→`he-prefs` 幂等迁移 + 清理** | 保留旧 key 仅新增走新 store | 推荐迁移 |

---

## 9. 发布门禁（与 M62 一致）
Typecheck / Build / Vitest / Freeze / Emoji / Structure / Visual / Consistency —— 8/8 PASS 方可发布 vM62.5。

> ADR 说明：M62.5 的 Freeze Revision Gate ADR（对应 W0）**将在 Implementation 启动后的 W0 阶段产出**，不在 Planning 阶段创建（避免提前落盘未批准的实现契约）。
