# M62.5 — W0-bis + W3 + W4 Preparation Review

> 模式：STRICT READ ONLY / PREPARATION AUDIT ONLY
> 基线：HEAD=vM62 (`68cd0fa`)，runtime=0.13.0，W1 Closure=APPROVED，Stage B Decision=APPROVED FOR PREPARATION / NOT APPROVED FOR IMPLEMENTATION
> 本文件仅记录审计结论，未修改任何源码、未修改 `scripts/freeze-check.mjs`、未 commit/push/tag。
> 生成时间：2026-07-29

---

## 1. Executive Summary

| 审计项 | 结论 |
|--------|------|
| W0-bis Freeze Supplement | 真实缺口 = **34 个顶层组件** + **`data/understandingRules.ts`**（精确文件枚举，无目录级扩大） |
| W3 Terminology Access Layer | `terminology.ts` 数据存在（9 概念，含 zh/en/ja），但 `getTermLabel()` **全仓 0 定义 / 0 消费** → 访问层缺失 |
| W4 Proper Name Display Policy | `getDisplayName()` 定义于 `preferences.ts:103`，**透传 no-op**，**0 消费**；`AppPreferences` 含 `contentDisplayMode` + `properNameMode` |
| Stage B Entry Gate | 7 项前置 **全部 OPEN**（未实现） |
| 最终结论 | **B. READY FOR PREPARATION ONLY**（禁止进入 Implementation）|

**最重要修正（相对 Stage B Planning Review）**：先前估计「~34 顶层组件需枚举」在本审计中被**精确核实为真实未放行集合**。大量组件已通过历史 gate（M30/M34/M35/M43–M61）单独放行，或位于已放行的子目录前缀（`discover/ exploration/ journey/ ai/ ui/ entity/ workspace/`）下。因此 W0-bis 的真实增量远小于预期——只需补齐未放行的 34 个顶层组件 + 个别数据文件。

---

## 2. W0-bis Freeze Supplement Gate Audit

### 2.1 当前 SCOPE_ALLOWLIST（frontend 部分，已全量核验）

`scripts/freeze-check.mjs` 中 `SCOPE_ALLOWLIST`（L161–L466）。frontend 相关条目分两类：

**A. 目录前缀（trailing `/`，放行其下全部嵌套文件）**
- `frontend/src/locales/`（M62.5 i18n 资源框架）
- `frontend/src/pages/`（含 `DiscoverPage.tsx` / `DevCatalog.tsx`）
- `frontend/src/components/discover/`
- `frontend/src/components/exploration/`
- `frontend/src/components/journey/`
- `frontend/src/components/ai/`
- `frontend/src/components/ui/`（含 `Button.tsx` / `Card.tsx`）
- `frontend/src/components/entity/`（含 `ViewSwitcher` / `ConnectionExplorer` / `EntityExperienceHeader` / `EntityInsightCard`）
- `frontend/src/components/workspace/`（含 `ExplorationPathCard`）
- `frontend/src/data/entity/`
- `frontend/src/data/ai/`
- `frontend/src/data/workspace/`
- `frontend/src/__tests__/`
- `frontend/src/styles/`

**B. 顶层精确文件（与 Stage B i18n 相关的已放行项）**
- `frontend/src/lib/format.ts`、`frontend/src/lib/preferences.ts`（M62.5）
- `frontend/src/components/LanguageSwitcher.tsx`（M62.5）
- `frontend/src/data/locale.tsx`（M62.5 升级）
- `frontend/src/data/provenanceApi.ts` + `.test.ts`（M30-A）
- `frontend/src/components/ProvenancePanel.tsx` + `.test.tsx`（M30-A）
- `frontend/src/components/EntityPage.tsx`（M30-A）
- `frontend/src/components/RelationshipView.tsx` / `RelationshipEvidence.tsx` + `.test.tsx` / `ExplorationFlowGuide.tsx` + `.test.tsx`（M30-B）
- `frontend/src/components/ConnectionsExplainedPanel.tsx`（M61）
- `frontend/src/components/AppShell.tsx` + `.test.tsx`（M34）
- `frontend/src/components/EntityHeader.tsx` + `.test.tsx`（M34）
- `frontend/src/components/GraphViewPanel.tsx` + `.test.tsx`（M34）
- `frontend/src/components/LandingPage.tsx` / `SearchBox.tsx`（M34）
- `frontend/src/components/FeedbackWidget.tsx` + `.test.tsx` + `lib/journey.ts` + `data/narrative.ts`（M35）
- `frontend/src/components/AIExplanationPanel.tsx` / `GroundedAnswer.tsx` / `CitationList.tsx` + `.test.tsx` + `data/aiClient.ts`（M43 AI）
- `frontend/src/components/Event*`（CausalChain/ImpactPanel/NarrativeCard/NarrativeJourney）+ tests（M50/M51/M52/M53 事件叙事）
- `frontend/src/components/HistorianChat.tsx` + test（M54 史家）
- `frontend/src/components/JourneyCard.tsx` + test（M55）
- `frontend/src/components/Research*`（Panel/DimensionCard/Report/Summary/MultiEntitySelector/BookmarkButton/Library/RecommendationCard/DiscoveryPanel）+ `data/Research*`（M56/M57/M58/M59 研究）
- `frontend/src/components/EntityPageShell.tsx` + test（M44/M45）

**核验结果（用户要求的禁止项）**
- ❌ 不存在 `frontend/src/components/**` 任何宽放 → 通过
- ❌ 不存在 backend 放行（本审计范围仅 frontend）→ 通过
- ❌ 不存在 dependency 放行 / runtime 放行 → 通过（runtime 锁定 0.13.0）

### 2.2 B1–B7 真实迁移面分类

**A. 已在 allowlist（Stage B 可直接改，无需新增条目）**
- 全部 `discover/ exploration/ journey/ ai/ ui/ entity/ workspace/` 子目录组件（目录前缀已覆盖）
- 上述 B 类顶层精确文件（EntityPage / RelationshipView / EntityHeader 等已是 B1/B3 主战场，已放行）
- `pages/DiscoverPage.tsx`（目录前缀已覆盖）
- `locales/` 资源（M62.5 已放行）

**B. 未放行但 Stage B 必需（W0-bis 精确枚举候选）**

> 原则：每条为**精确文件路径**，禁止目录级扩大，禁止 `frontend/src/components/**`。
> 用途标注：**「仅允许 i18n 字符串外置」**
> 禁止：业务逻辑修改 / API 修改 / 数据结构修改 / 枚举修改

34 个未放行顶层组件（按 Glob 全量比对 allowlist 得出，与先前估计数一致）：

1. `frontend/src/components/TimelinePanel.tsx`
2. `frontend/src/components/Breadcrumb.tsx`
3. `frontend/src/components/ConnectionsPanel.tsx`
4. `frontend/src/components/ContinueExploringPanel.tsx`
5. `frontend/src/components/CrossTopicBridge.tsx`
6. `frontend/src/components/CrossTopicConnectionsPanel.tsx`
7. `frontend/src/components/CrossTopicTopicList.tsx`
8. `frontend/src/components/EmptyState.tsx`
9. `frontend/src/components/EntityExplorationGuide.tsx`
10. `frontend/src/components/EntitySearchBox.tsx`
11. `frontend/src/components/ErrorCard.tsx`
12. `frontend/src/components/ExplorationJourney.tsx`
13. `frontend/src/components/ExplorationPathTree.tsx`
14. `frontend/src/components/ExplorationPathsPanel.tsx`
15. `frontend/src/components/ExplorationTrail.tsx`
16. `frontend/src/components/FeaturedTopics.tsx`
17. `frontend/src/components/FirstExplorationGuide.tsx`
18. `frontend/src/components/HistoryBar.tsx`
19. `frontend/src/components/InterpretationPanel.tsx`
20. `frontend/src/components/LoadingSkeleton.tsx`
21. `frontend/src/components/MainEntityCard.tsx`
22. `frontend/src/components/MultiEntityTimeline.tsx`
23. `frontend/src/components/RecentExplorations.tsx`
24. `frontend/src/components/RecommendationPanel.tsx`
25. `frontend/src/components/RelatedEntityList.tsx`
26. `frontend/src/components/SummaryPanel.tsx`
27. `frontend/src/components/TemporalComparisonPanel.tsx`
28. `frontend/src/components/ThemesPanel.tsx`
29. `frontend/src/components/TopicComparisonPanel.tsx`
30. `frontend/src/components/SearchResults.tsx`
31. `frontend/src/components/EntityPickerPanel.tsx`
32. `frontend/src/components/MultiEntityContextPanel.tsx`
33. `frontend/src/components/RelationshipPathGraph.tsx`
34. `frontend/src/components/RelationshipInsightPanel.tsx`

数据文件缺口（需在执行 W0-bis 时 per-file 确认是否含 UI 串）：
- `frontend/src/data/understandingRules.ts`（**未在 allowlist**；被 `EntityTabGuidance` 与 `ThemesPanel.test` 引用，疑似含导览文案）

> 说明：其余顶层 `data/*.ts`（`narrative` / `EntityTabGuidance` / `ResearchHistory` / `UIAudit` / `UserJourney` 等）均已单独放行，无需新增。W0-bis 执行时应以「实际被 B1–B7 改动的文件」为最终清单，但上述 34 + understandingRules.ts 为确定性起点。

### 2.3 测试文件影响（仅识别，不得提前修改）

i18n 外置会改变组件渲染文本，以下测试含字符串断言/snapshot，Stage B 期间需随实现同步更新（**不预先加入 allowlist**，遵循 W1 既定规则——测试文件仅在实际创建/修改时登记）：

- 与上述 34 组件对应的 `*.test.tsx`：`CrossTopicBridge.test` / `ExplorationJourney.test` / `ExplorationPathTree.test` / `RecommendationPanel.test` / `EntityPickerPanel.test` / `MultiEntityContextPanel.test` / `RelationshipPathGraph.test` / `RelationshipInsightPanel.test` 等
- `EntityHeader.test.tsx`（已放行组件，但其 `<h3>Entity</h3>` 硬编码英文属 W3 范畴，测试需同步）
- `ThemesPanel.test.tsx`、`understandingRules.test.ts`（数据文案外置影响）

### 2.4 Least Privilege 核验（针对 W0-bis 方案）

- 建议增量为 **34 精确文件 + understandingRules.ts**，均为逐文件枚举，**无 `frontend/src/components/**` 前缀** → 符合 Least Privilege
- 禁止项（写入 ADR 限定语）：业务逻辑 / API / 数据结构 / 枚举 一律不改
- 风险：若执行时滑向目录前缀（如 `frontend/src/components/`）即违反 R16 → 须在 W0-bis 执行单内显式约束

---

## 3. W3 Terminology Access Layer Audit

### 3.1 terminology.ts 当前结构

`frontend/src/locales/terminology.ts`（13 行）：

```ts
export const terminology = {
  Entity:       { zh: '实体',     en: 'Entity',       ja: '実体' },
  Relationship: { zh: '关系',     en: 'Relationship', ja: '関係' },
  Timeline:     { zh: '时间线',   en: 'Timeline',     ja: 'タイムライン' },
  Journey:      { zh: '历程',     en: 'Journey',      ja: '旅路' },
  Trust:        { zh: '信任',     en: 'Trust',        ja: '信頼' },
  Evidence:     { zh: '证据',     en: 'Evidence',     ja: '証拠' },
  Historian:    { zh: '史家',     en: 'Historian',    ja: '歴史家' },
  Discovery:    { zh: '发现',     en: 'Discovery',    ja: '発見' },
  Knowledge:    { zh: '知识',     en: 'Knowledge',    ja: '知識' },
}
```

- 数据结构：扁平对象，key=概念名，value={zh,en,ja}
- 包含 locale：✅ zh / ✅ en / ✅ ja

### 3.2 getTermLabel 状态

- 全仓搜索 `getTermLabel` → **0 文件命中**
- 结论：**Terminology Data Exists / Access Layer Missing**
- 影响：B1（Entity/Relationship 术语消费）、B2（Core Shell 术语）无法调用术语；`EntityHeader.tsx` 中 `<h3>Entity</h3>` 硬编码英文正是待接入点

### 3.3 边界审查（Boundary Confirmation）

| 概念 | 是否在 terminology.ts | 归属 |
|------|----------------------|------|
| Entity / Relationship / Timeline / Journey / Trust / Evidence / Historian / Discovery / Knowledge | ✅ 9 个均在 | 领域概念层 ✅ |
| Person / Location / Event / Organization / Artifact | ❌ 均不在 | 实体类型层（禁入 terminology）✅ |

→ 边界**当前干净**，无实体类型泄漏进 terminology。**W3 实现 `getTermLabel` 时须维持此边界**：只服务领域概念，实体类型经 `getEntityLabel(type, locale)` 处理。

### 3.4 entityLabels.ts 签名缺口

`frontend/src/data/entity/entityLabels.ts`：
- `ENTITY_TYPE_LABELS: Record<string,string>`（8 类型：Person/Civilization/Event/Location/Time Period/Technology/Religion/Idea）
- `getEntityLabel(type: string): string` → **无 locale 参数**（L29）
- `getEntityIcon(type: string): string`

→ **W3 / B1 扩展需求**：`getEntityLabel(type, locale)` 需支持 locale 回退（zh 兜底），且 `entityLabels.ts` 当前**未放行**（位于 `data/entity/` 目录前缀下 → 已放行，无需 W0-bis 条目）。

### Terminology Layer Gap Report

| 项 | 状态 |
|----|------|
| 术语数据 | ✅ 存在（9 概念 × 3 locale） |
| `getTermLabel(id, locale)` | ❌ 缺失（W3 实现） |
| 实体类型标签边界 | ✅ 干净（entityLabels.ts 独立） |
| `getEntityLabel` locale 支持 | ❌ 需扩展（W3/B1） |

---

## 4. W4 Proper Name Display Policy Audit

### 4.1 getDisplayName 状态

- 定义：`frontend/src/lib/preferences.ts:103`
- 实现：**透传 no-op**（`return name`）
- 全仓消费：grep `getDisplayName` → **仅定义处 1 处，0 消费**

### 4.2 AppPreferences 字段核验

`preferences.ts:9–20`：
```ts
export interface AppPreferences {
  language: Locale
  contentDisplayMode: ContentDisplayMode   // 'original' | 'localized' | 'dual'
  properNameMode: ProperNameMode           // 'original' | 'localized' | 'dual'
  aiPreference: AIPreference
  future?: { theme?; accessibility?; motion? }
}
```
→ ✅ 含 `contentDisplayMode` 与 `properNameMode`（比 Rev2 §2.10 的 `contentMode` 更细，为 as-built 偏差，已记入 Decision Record）

### 4.3 no-op 确认

- `getDisplayName(name, _locale, _mode)` 当前忽略 locale/mode，直接返回 name
- UI 渲染未受其影响（因为尚未接线）→ 确认 Axis2 未接入

### 4.4 实体名称直接渲染扫描（重点文件）

| 文件 | 渲染点 | 分类 |
|------|--------|------|
| `EntityPage.tsx` | `entity.name` / `viewModel.identity.name` / `r.other.name`；`nameById` 映射；`SummaryPanel title` / `entityName` / `centerEntityName` 透传 | **A**（专名数据，应经 `getDisplayName`） |
| `MainEntityCard.tsx` | `<span className="me-name">{mainEntity.name}</span>` | **A**（可见专名，应经 `getDisplayName`） |
| `RelatedEntityList.tsx` | `displayName = nameById?.[item.id] ?? item.id` → `<span className="re-name">{displayName}</span>`；`aria-label="Explore ${displayName}"` | **A**（可见专名）+ aria 动词部分属 i18n 串 |
| `RelationshipView.tsx` | `<span className="rel-root-name">{mainEntity.name}</span>`；`displayName = nameById?.[item.id] ?? item.id` → `<span className="rel-target-name">{displayName}</span>`；aria `Focus/Explore/查看…的依据 ${displayName}` | **A**（专名）+ aria 动词属 i18n 串 |
| `EntityHeader.tsx` | `<h3>Entity</h3>`（硬编码英文术语）+ `<span className="re-type">{type}</span>`（实体类型码） | `<h3>Entity</h3>`→W3 `getTermLabel`；`{type}`→W3/B1 `getEntityLabel(type,locale)` |
| `EntityPageShell.tsx` | Tab `label:'了解'/'研究'/'扩展'` + `ariaLabel` 中文长句 | **B-class i18n 串**（非专名，经 `t()`） |

**A 类（应进入 `getDisplayName`）**：所有实体专名可见渲染（MainEntityCard / RelatedEntityList / RelationshipView / EntityPage 的 name 透传）。
**B 类（保留原始数据展示）**：
- `console.log` 调试行（EntityPage:187,190）→ 非 UI，可忽略/移除
- `nameById` 映射构建（数据管道，非展示）
- 实体 `type` 字段 → 走 `getEntityLabel(type, locale)`，**不**走 `getDisplayName`
- aria-label 中的动作动词（"Explore"/"Focus"/"查看…的依据"）→ 走 `t()`，其中嵌入的专名走 `getDisplayName`

### Proper Name Policy（三模式，W4 实装）

```
original:  显示原始名称
localized: 显示本地化名称；无本地化则 fallback original
dual:      显示「中文 / Original」
```
**硬规则**：实体专名**禁止进入 `t()`**；一律经 `getDisplayName(name, locale, properNameMode)`。

---

## 5. Stage B Entry Gate 当前状态

| # | 前置条件 | 状态 |
|---|----------|------|
| 1 | W0-bis Freeze Gate PASS | ❌ OPEN（枚举已识别，未执行） |
| 2 | `getTermLabel` 完成 | ❌ OPEN（W3 未实现） |
| 3 | `getDisplayName` 三模式完成 | ❌ OPEN（W4 未实现） |
| 4 | Proper Name Policy 接线完成 | ❌ OPEN（W4 未接线） |
| 5 | Entity Type Label 与 Terminology 边界确认 | ✅ 边界已确认干净（见 §3.3）；`getEntityLabel` locale 扩展待 W3/B1 |
| 6 | 中文硬编码扫描完成 | ⚠️ 部分（B1–B7 实施时执行） |
| 7 | 英文 UI 硬编码二次扫描完成 | ❌ OPEN（R14 盲区，见 §6） |

→ **7 项中 1 项边界已清、1 项部分、5 项 OPEN** → 远未达进入 B1 阈值。

---

## 6. Risk Update

| 风险 | 状态 | 说明 |
|------|------|------|
| R1–R13（W1 结转） | 🟢 GREEN | 继承自 W1 Closure / Decision Record，无新恶化 |
| **R14 英文 UI 硬编码** | 🔴 CONFIRMED | 二次扫描实证：未放行顶层组件含英文硬编码 UI 串，例如 `EntityExplorationGuide.tsx:42` `<h3>Explore {entityName}</h3>`、`FirstExplorationGuide.tsx:40` `<h3>Explore {title}</h3>`、`SearchResults.tsx:139` `<h3>Search results for "{query}"</h3>`、`TimelinePanel.tsx:67` `<h3>Timeline</h3>`、`RelationshipView.tsx:60` `<h3>Relationship Network</h3>`（已放行）、`ProvenancePanel.tsx:91` `<h3>Evidence / Provenance</h3>`（已放行）。首轮 `\p{Han}` 扫描确实漏掉英文串 → 真实迁移面 >102 文件，须全量英文串审计（建议作为 Stage B W10 任务） |
| **R15 W3/W4 未完成阻塞 B1/B3** | 🔴 OPEN（已列为 Phase 2 前置） | `getTermLabel` 缺失 → B1 术语消费不可行；`getDisplayName` no-op → B3 专名策略不可行。正是 Phase 2 要补的内容 |
| **R16 W0-bis Least Privilege** | 🟢 GREEN（方案层面） | 建议增量 = 34 精确文件 + understandingRules.ts，**无目录前缀**，符合 Least Privilege。执行时若滑向 `components/**` 即破防，须在执行单显式约束 |

---

## 7. Recommendation

**最终结论：B. READY FOR PREPARATION ONLY**

理由：
1. W0-bis 枚举已**精确核实**（34 顶层组件 + understandingRules.ts），方案符合 Least Privilege，但尚未执行；
2. W3 访问层（`getTermLabel`）与 W4 专名策略（`getDisplayName` 三模式 + Axis2 接线）**均未实现**；
3. Stage B Entry Gate 7 项前置中 5 项 OPEN，未达进入 B1–B7 的阈值。

**建议下一步（待 PO 拍板后进入实现）**：
- **Phase 1**：执行 W0-bis——将 §2.2 的 34 文件 + understandingRules.ts 逐条加入 `SCOPE_ALLOWLIST`（精确路径，附「仅允许 i18n 字符串外置」ADR 限定语），跑 freeze-check 验证 PASS。
- **Phase 2**：Stage A 收尾——W3 实现 `getTermLabel(id, locale)` 并把 `getEntityLabel(type)` 扩为 `getEntityLabel(type, locale)`；W4 实装 `getDisplayName` 三模式并接线 Axis2。
- **Phase 3**：Stage B 按 B1→B7 固定顺序迁移（仅 `t()` / `getTermLabel()` / `getDisplayName()`，禁硬编码），W10 完成中/英文串全量审计。

**结束状态：等待 PO Decision。不得执行任何代码修改。**

---

*附：本审计全部基于只读 grep / 读取，未触达任何实现文件。所有「应实现」「应扩展」均为规划结论，非已落地改动。*
